# 2026-06-15 工作日志

## gitPush 提交页面修复
- **文件**: `src/features/gitPush/components/WorkingTreePanel.vue`
- **修改 1**: `rows="3"` → `rows="10"`，提交信息 textarea 从 3 行扩展到 10 行
- **修改 2**: `.wt-commit-msg` 样式添加 `box-sizing: border-box`，修复 `width: 100%` + padding + border 导致右侧溢出卡片容器的问题

## gitPush 刷新性能优化
- **文件**: `src/features/gitPush/index.vue` (`handleRefresh`)
- **问题**: 单项目刷新 6 步完全串行（refreshRemotes → loadPushStatus → loadWorkingTree → loadCommitLog → loadBranches → loadStashList），大仓库或内网环境下 `git update-index --refresh` 等命令慢时会阻塞整个刷新
- **修改**: 改为 2 组并行执行，与 `silentRefreshAll` 保持一致模式 — 第 1 组 `Promise.all([refreshRemotes, loadPushStatus])`，第 2 组 `Promise.all([loadWorkingTree, loadCommitLog, loadBranches, loadStashList])`
- **关键发现**: 刷新流程所有 git 命令都是本地操作（`git status`、`git log`、`git branch` 等），不依赖网络。慢的原因是大仓库 `git update-index --refresh` 的 I/O 耗时 + 串行阻塞模式

## gitPush 批量刷新批次化（修复 silentRefreshAll 拥堵）
- **文件**: `src/features/gitPush/index.vue` + `src/features/gitPush/types/index.ts`
- **问题**: `silentRefreshAll`/`onMounted`/watchers 中 `Promise.all(projList.map(...))` 让所有项目同时涌入 3 槽位 git 信号量，10 个项目 × 8 命令 = 80 个命令排队。`checkPushStatus` 内部对 remote 的检查也是串行 for 循环
- **修改 1** (index.vue): 添加 `batchProcess` 工具函数，将 `silentRefreshAll`、`onMounted` 定时器、`watch(activeCategory)`、`watch(currentView)` 四处批量刷新点全部改为 `batchProcess(items, 3, fn)` 批次处理（每批 3 个项目匹配信号量上限）
- **修改 2** (types/index.ts): `checkPushStatus` 中 3 个 remote 的检查从串行 `for...await` 改为 `Promise.all(remoteChecks.map(...))` 并行执行，减少单项目内串行等待

## FeatureDrawer "不常用"标记图标不可见修复
- **问题**: 用户标记功能为"不常用"后，该项目移至底部"不常用"分组，但该分组默认折叠 (`rarelyExpanded = ref(false)`)，导致看不到书签填充图标
- **修改 1** (`FeatureDrawer.vue`): `rarelyExpanded` 默认值改为 `true`（默认展开）；添加 `watch(hasRarelyUsed)` 监听，当所有不常用项目恢复后自动折叠
- **修改 2** (`index.scss`): 书签按钮默认 `opacity` 从 `0.35` 提升到 `0.6`，提高可见度

## FeatureDrawer 整体 UI 放大 50%
- **文件**: `FeatureDrawer.vue` + `styles/index.scss`
- 抽屉宽度 280→420px，最大高度 260→390px
- 所有内部元素等比放大：header/列表 padding、图标尺寸（25→38、14→21、12→18）、按钮尺寸（24→36、22→33、18→27）、字号（13→20、10→15、12→18）

## FeatureDrawer 内部元素回退 + 网格改4列
- 容器保留 420×390，内部所有元素恢复原始尺寸
- 网格模式 `grid-template-columns: repeat(3, 1fr)` → `repeat(4, 1fr)`

## FeatureDrawer 角标替换按钮
- 固定/不常用双按钮（带边框）→ 无边框角标（hover 显示，active 常显）
- 固定角标：`ph:push-pin-simple` / `ph:push-pin-simple-fill`（12px）
- 不常用角标：`ph:eye-slash`（12px）
- 不常用分组改回默认折叠 (`rarelyExpanded = ref(false)`)

## FeatureDrawer 搜索过滤 + 分组标签（重构为顶部标签栏）
- **接口**: `FeatureDrawerItem` 保留可选 `group?: string` 字段
- **数据**: index.vue 中所有项移除 group 赋值，默认无分组（后续用户自定义）
- **标签栏**: 搜索栏下方，显示"全部" + 各唯一分组标签（胶囊圆角按钮），有 ≥2 个分组时才显示
- **过滤**: `activeGroup` ref 控制当前选中标签，`displayItems` 按标签过滤；搜索模式忽略标签
- **样式**: 胶囊 pill 风格标签（`border-radius: 12px`），激活态 primary 主题色高亮

## AttrsPanel "前往发布" 复制标题+内容（模仿 SyncCaster）
- **文件**: `src/features/docAnalysis/components/AttrsPanel.vue`
- **变更**: 点击"前往发布"按钮不再仅 `window.open(url)`，改为先调用 `exportMdContent` 获取文档 MD 内容，组合 `# 标题\n\n正文` 到剪贴板，弹出 toast 提示后 300ms 延迟打开平台 URL
- **新增**: `handlePublishGo(platform)` 函数 + `publishGoLoading` 加载状态 + 按钮 `:disabled` 和 CSS `.spin-icon` 旋转动画
- **删除**: 旧的 `openPlatformUrl(url)` 函数
- **目的**: 用户跳转到平台后直接 Ctrl+V 即可粘贴标题+全文，与 SyncCaster 行为对齐

## gitPush 统计视图新增"未设置平台项目"列表 + 列表滚动优化
- **修改 3 个文件**: `useGitPush.ts`、`index.vue`、`StatsPanel.vue`
- **useGitPush.ts**: 新增 `noPlatformProjects` computed（过滤 `!githubUrl && !giteeUrl && !giteaUrl` 的项目），置于 `uncommittedProjects` 之后
- **index.vue**: 解构 `noPlatformProjects` 并传递 `:no-platform-projects` prop 给 StatsPanel
- **StatsPanel.vue**: 新增"未设置平台的项目"区块（项目名 | 添加时间表格，`formatDate` 辅助函数），位于推送状态分布之后；为 `.gp-table-wrap` 添加 `max-height: 220px; overflow-y: auto` 实现固定高度可滚动；新增 `--time` 单元格样式；后续添加整行点击导航（`gp-table-row--clickable` + `@click="emit('viewProject', item.id)"`）

## SCSS 全量分离：Vue 文件内联样式 → styles/ 外部文件
- **涉及 10 个 feature，20 个 Vue 文件**：docAnalysis×5（AttrsPanel/StatsOverview/FilterSettings/DocListItem/DateInput）、gitPush×4（index/WorkingTreePanel/StatsPanel/BranchCommitList）、aiContentGenerator×2、video×3、shortcut×4、generalSettings×1、superPanel×1
- **模式**：每个 Vue 文件 `<style lang="scss" scoped>` 仅保留 `@use "../styles/<Component>.scss"`；样式代码移至 `styles/<Component>.scss`
- gitPush 因文件巨大（index.vue 2955 行含 1115 行 SCSS）使用 Node.js 脚本批量提取
- **CLAUDE_RULES.md** 新增「强制规则：SCSS 必须分离到 styles/ 目录」章节，包含正确/错误示例、目录结构规范

## passwordVault 模块审查与重构
- **涉及 7 个文件**: `index.ts`, `index.vue`, `types/index.ts`, `utils/crypto.ts`, `components/PasswordVaultLogin.vue`, `styles/login.scss`, `statusBar/index.vue`, `floatingBox/tools/passwordVault.ts`
- **CLAUDE.md 合规修复 3 项**:
  1. `types/index.ts` 移出运行时逻辑（ref/4个函数）→ `index.ts`，types 文件仅保留类型+Manager
  2. `index.vue` 本地 `navigator.clipboard.writeText` → `copyToClipboard` from `@/utils/domUtils`（含 fallback 优势）
  3. `PasswordVaultLogin.vue` emoji 🔐 → `<IconWrapper name="pageLock" :size="48">`
- **死代码清理 2 项**:
  1. `index.vue` `encryptionSalt` ref（6 处只写不读）全部移除
  2. `crypto.ts` `generateVerifySalt()` 无意义包装移除，统一用 `generateSalt()`
- **性能优化**: `handleLogin` 中 `loadEntries()` + `loadCategories()` 从串行改为 `Promise.all` 并行
- **冗余代码**: `PasswordVaultLogin.vue` `watch(isFirstTime, {immediate})` 替换为 `onMounted`（isFirstTime 与组件生命周期同频，watcher 等价于初始化逻辑）
- **依赖路径修正**: `statusBar/index.vue` 和 `floatingBox/tools/passwordVault.ts` 从 `../passwordVault/types` 改为 `../passwordVault`（不再跨层引用 types 内部文件）
- **构建验证**: lint 零错误，`pnpm vite build` 通过

## passwordVault UI 对齐 Codex 规则重构
- **涉及 4 个 SCSS 文件**: `mixins.scss`, `index.scss`, `help.scss`, `login.scss`
- **移除 3 处 `box-shadow`** (Codex 禁用阴影，改用边框):
  - `.password-vault-dialog` 的 `0 20px 60px` → `border: 1px solid`
  - `.entry-card:hover` 的 `0 2px 8px` → `border-color: var(--b3-theme-primary)`
  - `.help-dialog` 的 `0 20px 60px` → `border: 1px solid`
- **Codex Token 替换**（硬编码 → `$vp-*`/`$radius-*`/`$spacing-*`）: 6px→`$vp-radius`, 12px→`$radius-lg`, 8px→`$radius-md`/`$spacing-sm`, 16px→`$spacing-lg`, 20px→`$radius-full`(pill), 4px→`$radius-sm`
- **字体对齐**: `font-family: monospace` → `$vp-mono` (等宽字体栈), `.field-label`/`.form-group label` → `@include uppercase-label` (Codex 10px/700/uppercase/opacity:0.45)
- **交互增强**: hover 边框变色（entry-card/entry-account/category-chip/category-item）、entry-actions 添加 `transition: opacity`、category-chip 添加 `transition: all`、`input-base` focus 添加 `box-shadow: 0 0 0 2px` Codex 发光环
- **配色统一**: `$brand-orange` → `$brand-destructive` (error-message/password-hint-display)
- **死代码清理**: 删除 3 条废弃 CSS 注释 (`.category-name-input`/`.add-category-btn`/`.delete-category-btn`)
- **新增**: `mixins.scss` 定义 `$vp-mono` 局部变量 + `@mixin uppercase-label`
- **构建验证**: lint 零错误，`pnpm vite build` 通过

## Codex Token 全局化 + 规则权威化
- **`src/_variables.scss`**: 新增 Codex 语义别名 — `$vp-radius: 6px`, `$vp-mono` (等宽字体栈), `$spacing-xs/sm/md/lg` (4/8/12/16px)，所有 feature 通过 `@use '@/variables.scss'` 统一导入，**禁止模块本地重复定义**
- **`passwordVault/styles/mixins.scss`**: 移除本地重复变量声明（9 行），替换为注释引用全局来源
- **`CLAUDE_RULES.md` § UI 风格 Codex**: 重写为强制规范 — 顶部声明 Token 来源 + 速查表（规则×CSS）+ `.vp-*` 模式库（弹窗/输入框/标签）+ 禁止事项表（6 条 ❌→✅），末尾注明 "所有新增 feature 必须遵循"
- **`CLAUDE.md`**: 摘要同步更新，强调全局 Token 和禁止硬编码

## passwordVault 存储审查 + 加密扩展 + 路径展示
- **涉及 7 个文件**: `types/storage.ts`, `types/index.ts`, `utils/crypto.ts`, `index.vue`, `components/HelpDialog.vue`, `styles/help.scss`
- **存储审查结论**: 6 key 分离设计无逻辑缺陷，verifySalt/encryptionSalt 分离增加安全层次；核心问题是 entries 内仅 password 加密
- **加密扩展（核心变更）**:
  - `StoredPasswordEntry` 旧格式 `{ encryptedPassword, iv, name, account, description }` → 新格式 `{ encryptedPayload, iv }`（`name`/`account`/`password`/`description` 整体 AES-GCM 加密为一个 blob）
  - 新增 `encryptEntryPayload()` / `decryptEntryPayload()` 整体加解密函数
  - 新增 `LegacyStoredPasswordEntry` 辅助类型 + `DATA_VERSION = 2` 常量
  - `loadEntries()` 自动检测 `encryptedPassword` 字段，旧格式透明迁移回写新格式
  - `saveEntries()` / `handleChangePassword()` 重加密循环均适配新格式
- **存储路径展示**:
  - `HelpDialog.vue` 新增 `storagePath` prop + 「数据位置」section，点击复制路径
  - `index.vue` 新增 `storagePath` computed（plugin.dataDir 拼接）
  - `help.scss` 新增 `.storage-path-section` 样式（Codex 边框卡片 + `$vp-mono` + hover 高亮）
- **死代码清理**: 删除 `PasswordVaultData` 接口（定义后从未使用）
- **构建验证**: lint 零错误，`pnpm vite build` 通过

## universal-arch-skill 通用架构规范 Skill 生成
- **输出**: `.codebuddy/skills/universal-arch-skill.skill`（32 文件，约 85KB）
- **内容结构**:
  - `SKILL.md`（7KB）— YAML frontmatter + 三种工作模式（校验/脚手架/审查）+ 资源索引
  - `references/arch-principles.md` — 6 大通用架构原则（语言无关）
  - `references/vue-ts-adapter.md` — Vue/TS 适配（8 步注册清单、Dock/Modal 模式、Codex UI、SCSS 分离）
  - `references/csharp-adapter.md` — C# 适配（DI 注册、枚举+源码生成器、XAML Token、DesignTokens 常量）
  - `references/js-ts-adapter.md` — 纯 JS/TS 适配（Service 模式、EventBus、配置管理）
  - `scripts/validate-project-structure.py` — 项目结构校验器（5 类检查：目录/注册/国际化/禁止模式/样式分离）
  - `scripts/scaffold-feature.py` — 功能模块脚手架生成器（模板变量替换 + 注册清单输出）
  - `assets/feature-template-vue/` — 7 个 Vue 模板（index.ts/vue + types/ + styles/ + README）
  - `assets/feature-template-csharp/` — 4 个 C# 模板（Service/Model/Registration）
  - `assets/feature-template-js/` — 3 个 JS 模板（index/types/README）
- **触发词**: 架构规范、项目结构校验、功能模块生成、脚手架生成、代码审查、arch review、feature scaffold 等
- **Python 兼容**: 添加 `from __future__ import annotations`，兼容 Python 3.7+
- **打包**: 因 Windows GBK 编码问题无法使用 skill-creator 的 package_skill.py，改用 `shutil.make_archive` + 重命名为 .skill

## floatingToolbar ↔ passwordVault 联动（存密码按钮）
- **解耦架构**: 浮动工具栏 → `emitCustomEvent("openPasswordVaultAdd")` → App.vue → `openPasswordVaultWithText(text)` → `pendingEntryName` ref → passwordVault watch 自动填充
- **新建文件**: `floatingToolbar/core/actions/passwordVault.ts` — `createPasswordVaultAction()` 使用 `createDialogAction` 工厂，锁形 SVG 图标，事件名 `openPasswordVaultAdd`
- **修改 floatingToolbar**: `core/actions/index.ts` 导出新 action，`index.ts` 按 `enablePasswordVault` 条件注册 `floatingToolbar.registerAction(createPasswordVaultAction(plugin))`
- **修改 passwordVault**: `index.ts` 新增 `pendingEntryName` ref + `openPasswordVaultWithText(text)` 函数；`index.vue` 新增两个 watch（`pendingEntryName` 变化且已登录时自动打开添加表单预填名称；`isLoggedIn` 变为 true 时检测 pending 数据后自动打开添加表单）
- **修改 App.vue**: 导入 `openPasswordVaultWithText`，注册 `window.addEventListener("openPasswordVaultAdd", ...)` 监听
- **修改 features/index.ts**: 新增导出 `openPasswordVaultWithText`、`pendingEntryName`
- **构建验证**: lint 仅预存警告（编译时断言），`pnpm vite build` 通过

## passwordVault 登录界面简化
- **涉及 2 个文件**: `components/PasswordVaultLogin.vue`, `styles/login.scss`
- 移除 5 处 IconWrapper：标题栏 settings、中央 pageLock 大图标、密码提示 info、错误消息 error、提示设置按钮 info
- 移除 `IconWrapper` import（不再使用）
- 删除 `.login-icon` SCSS 区块 + `.password-hint-display` 中 `:deep(.icon-wrapper)` 残留样式
- **构建验证**: lint 零错误，`pnpm vite build` 通过

## CLAUDE.md 新增跨功能联动规则
- 在「统一入口原则」与「硬规则」之间插入新章节「跨功能联动规则（强制）」
- **6 条硬规则**：
  1. Feature 间零直接导入（禁止 `import from @/features/*` 子目录）
  2. 单向数据流（发起方只 emitCustomEvent，不触碰响应方状态）
  3. App.vue 是唯一调度中心（所有跨功能事件监听统一注册）
  4. Public API 契约（响应方 index.ts 导出的函数/ref 即 public API）
  5. 事件名 camelCase 动词短语（如 `openPasswordVaultAdd`）
  6. 数据透传（event detail 由 App.vue 透传，双方不共享类型）
- **含正确/错误模式对比** + 基于 floatingToolbar↔passwordVault 的完整代码示例
- **架构图**：Feature A → emitCustomEvent → App.vue addEventListener → Feature B public API

## 存密码自动格式化解析
- **新建** `passwordVault/utils/parser.ts` — `parsePasswordText(text)` 自动检测分隔符拆解文本
- **分隔符优先级**: 中文冒号(`：`) → 英文冒号(`:`) → 下划线(`_`) → 短横线(`-`) → 竖线(`|`) → 逗号(`,`) → Tab(`\t`) → 空格（兜底）
- **字段映射**: 1 字段→name、2 字段→account+password、3+ 字段→account+password+description
- **数据结构重构**: `pendingEntryName: Ref<string>` → `pendingEntryData: Ref<PendingEntryData | null>`（`{ name, account, password, description }`）
- **修改文件**: `passwordVault/index.ts`（ref 类型+`openPasswordVaultWithText`调用解析器）、`index.vue`（两个 watch 预填全部字段而非仅 name）、`features/index.ts`（导出 `pendingEntryData`）、`utils/parser.ts`（新增 `PendingEntryData` 类型别名）
- **构建验证**: lint 零新增错误，`pnpm vite build` 通过
