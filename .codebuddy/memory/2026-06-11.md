# 2026-06-11

## themeColor 模块重构

- **消除 `id` 字段冗余**：从 `ThemeColorScheme` 接口和 4 个主题条目中移除重复的 `id` 字段，键名即为方案 ID
- **消除 `primaryRgb` 冗余**：新增 `hexToRgb()` 工具函数，自动从 hex 推导 RGB 字符串，无需手动维护
- **`removeTheme()` → `clearTheme()`**：更名并保持对外 `{ destroy }` 接口不变
- **superPanel 对齐**：`Object.values(THEMES)` → `Object.entries(THEMES)`，从 key 提取 id
- **README 更新**：补充 sakura/codex 到已支持表格，修正扩展示例，追加 4 条扩展建议
- **类型检查通过**，无新增 lint 错误，对外 API 完全兼容

## wordQuery 模块重构（6 项改动，11 个文件）

### P0 修复
- **剪贴板统一入口**：index.vue 中自定义 `navigator.clipboard.writeText` 替换为 `@/utils/domUtils` 的 `copyToClipboard`（4 处调用点），符合 CLAUDE_RULES.md 规范
- **补充 onunload 清理**：src/index.ts 中增加 `__wordQuery.destroy()` 调用

### 冗余消除
- **删除 `utils/apiBase.ts`**（纯 re-export 层）：4 个文件（api.ts / codeUtils.ts / codeTranslation.ts / useCodeFeature.ts）改为直接从 `@/utils/aiApi` 导入
- **合并 `FIELD_MAPPINGS` + `CONTENT_PATTERNS`**（~40 行重复）：在 FIELD_MAPPINGS 条目中增加 `contentKey`/`contentPatterns` 字段，删除独立常量，更新 `extractContentParts` computed
- **统一语言映射**：提取 `LANGUAGE_MAP` 到 types/index.ts，index.vue 和 translateText 方法共用单一数据源，消除两处重复定义

### 类型安全
- **`Props.i18n` 收紧**：`Record<string, any>` → `Record<string, any> & { wordQuery?: Record<string, string> }`
- **`ExtractedParts` 增加索引签名**：修复动态 key 访问类型错误

### 文档
- **README 更新**：补充 6 种功能模式说明、配置项、5 条扩展建议

- **类型检查通过**，零新增 lint 错误，外部 API 完全兼容
