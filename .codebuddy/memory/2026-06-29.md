# 2026-06-29

## gitPush 统计视图审查与修复

对 `src/features/gitPush/` 的统计视图模块进行了全面代码审查和修复，涉及 5 个文件。

### 修复项（ESLint + tsc 验证通过）

| 类别 | 问题 | 修复 |
|------|------|------|
| 重复定义 | `PlatformStatusItem` 在 StatsPanel.vue 和 useGitStats.ts 各定义一份 | 移至 `types/index.ts` 统一导出，两处改为 import |
| 类型绕过 | `(item as any)[pm.key]` 绕过 TypeScript 类型检查 | 新增 `getPlatformStatus(item, key)` 辅助函数，利用 `PlatformKey` 联合类型 |
| 硬编码 | 覆盖率列表手工编写 GitHub/Gitee/Gitea/CNB 四个块 | 改用 `v-for="pm in PLATFORM_META"` 循环生成 |
| 废弃代码 | `noPlatformProjects` 标记 @deprecated 但仍在 props/computed/传递中存活 | 从 StatsPanel props、index.vue 绑定/解构、useGitStats computed、useGitPush 转发全部移除 |
| 死字段 | `PendingProjectItem.isPending` 永远为 true 且从未读取 | 从接口和两处赋值删除 |
| 死字段 | `remoteCoverage.noRemote` 计算但模板未渲染 | 从 useGitStats computed 和 StatsPanel 接口删除 |
| 死字段 | `pushStatusStats.total` / `remoteCoverage.total` 与 `projectCount` 冗余 | 删除，pct() 改用 projectCount |
| 未消费 | `recentCommits`/`tagStats`/`statusStats`（useGitPush 未转发） | 从 useGitStats computed/return 删除 |
| 未消费 | `archivedProjects`（useGitPush 转发但 index.vue 未使用） | 从 useGitStats 和 useGitPush 链式删除 |
| 未使用参数 | `useGitStats` 的 `commitLogs` 参数 | 随 recentCommits 删除而清理 |
