# 2026-07-10

- 修复 `src/features/gitPush/components/ProjectCard.vue` 中 emits 声明重复标识符 `openProjectGitConfig`（第 688-689 行），删除重复行，lint 通过。
- 对 `src/features/floatingBox/` 进行全面代码审查与重构（14 个文件涉及 12 个文件修改 + 1 个新增）：
  1. **文件头注释**：为 7 个 `tools/*.ts` 补全缺失的文件头注释，符合 AGENTS.md 强制规则
  2. **消除重复杂代码**：新增 `tools/utils.ts` 导出 `createEventDispatchTool()` 通用工厂函数，4 个工厂文件（flashcardReading/passwordVault/superPanel/textDiff）从 17 行退化为 17 行结构化调用
  3. **refresh.ts 重复消除**：父级 action 与 refresh-ui 子项共用提取的 `refreshUI` 函数
  4. **index.vue 修复**：移除未使用的 `wrapperRef`；修正 `@click="isMobile && toggleExpanded()"` 为箭头函数形式
  5. **类型安全**：`ToolItem.vue` 的 `plugin` prop 从 `any` → `Plugin`；`index.ts` 的 `__floatingBox` 改为命名接口 `FloatingBoxInstance`
  6. **SCSS Token 合规**：`styles/index.scss` 中 20+ 处硬编码值替换为全局 Token（font-size/font-weight/border-radius/padding/gap/right/min-width）
  7. **i18n 统一**：`prompts.ts` 的 i18n 读取路径从 `plugin.i18n.prompts` 统一为 `plugin.i18n.floatingBox`
  8. **ToolItem.vue 优化**：提取 `hasChildren` computed 消除模板中重复的 `tool.children?.length` 判断
  所有改动保持业务逻辑零变化、事件名不变、导出签名不变。
- 修复 `PronunciationDialog.vue` 代码冗余和样式硬编码残留（6 项）：
  1. 删除死代码 `matchedCard` ref（声明 + 2 处赋值，从未被读取）
  2. 提取 `buildPrompt` 中英文分支公共后缀为 `PROMPT_SUFFIX` 常量（消除 4 行重复）
  3. `handleCategorySelect` 薄壳函数改为 `watch(selectedCategory, ...)`
  4. `categoryOptions` 占位选项改用 `t()` 读取 i18n
  5. SCSS 3 处硬编码修复（`8px`→`g.$spacing-2`、`12px`→`g.$spacing-3`、`16px`→`g.$spacing-4`）
  6. 移除模板 L55-56 多余空行
  lint 0 error。
