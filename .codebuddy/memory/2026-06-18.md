# 2026-06-18

## StatusBar 监控项图标移除 + 间距优化

去掉 6 个监控项（文档数/总字数/今日活动/CPU/内存/运行时间）的 icon prop（MonitorItem 组件 icon 可选，v-if="icon" 自动隐藏）。同时将 `.status__resUsage` gap 从 3px 增大到 6px，`.monitor-value` 添加 `padding: 0 2px` 提升纯文本项可读性。功能抽屉中的图标保留不变。

## StatusBar 监控项独立显隐开关

为状态栏 6 个监控项（文档数/总字数/今日活动/CPU/内存/运行时间）添加独立显示/隐藏控制。在功能抽屉中复用现有 pin 交互，通过图钉图标切换每个监控项显隐。

**修改文件**：
- `src/i18n/zh_CN/statusBar.json` — 新增 6 个监控项标题键（monitorNotes/monitorWords/monitorToday/monitorCpu/monitorMemory/monitorUptime）
- `src/i18n/en_US/statusBar.json` — 同上英文翻译
- `src/features/statusBar/index.vue` — 核心变更：
  - 模板每个 MonitorItem 加 `v-if="visibleMonitors.has(id)"`
  - FEATURES 数组新增 6 个监控项（pinnable:true, group:"监控", 无 shortcut/action）
  - 新增 `visibleMonitors`（reactive Set）状态 + `MONITOR_IDS` 常量
  - `handleToggleStatusBar` 分流：监控项 → visibleMonitors，快捷方式 → statusBarShortcuts
  - `handleToggleRarelyUsed` 同步处理监控项显隐
  - 新增 `statusBar-monitors` PluginStorage 持久化键（默认全显）
  - `statusBarVisible` computed 合并快捷方式+监控项供 FeatureDrawer 显示 pin 状态
  - `FeatureRegistryEntry.action` 改为可选（监控项无点击动作）

**未修改文件**：FeatureDrawer.vue、MonitorItem.vue、styles/index.scss（复用现有组件和样式）

构建验证通过（pnpm build + pnpm i18n:verify）。

## GitPush 推送/拉取输出可复制

在 `gp-output` 区域右上角添加悬停显示的复制按钮，点击调用 `copyToClipboard` 将输出文本写入剪贴板。修改 2 个文件：`index.vue`（模板 + `handleCopyOutput` 函数）、`styles/index.scss`（`.gp-output-copy` 样式）。零新增 lint 错误。

## GitPush 添加/导入合并为 + 下拉菜单

将头部两个按钮（"添加" + "导入"）合并为一个 `+` 图标按钮，点击展开下拉菜单：
- **添加单个项目** → 打开 AddProjectDialog
- **批量导入** → 打开 ScanImportDialog

修改 2 个文件：`index.vue`（模板合并 + `showAddMenu` ref + `closeIdeMenuOnOutside` 扩展）、`styles/index.scss`（`.gp-add-wrap` / `.gp-add-popover` / `.gp-add-item` 样式）。复用已有的 `document.addEventListener("click", closeIdeMenuOnOutside)` 处理点击外部关闭。零新增 lint 错误。

## GitPush 卡片点击刷新 git 状态

在 `.gp-card` 上添加 `@click="handleCardClick(project.id, $event)"`，通过 `closest("button, input, select, textarea, .gp-ide-popover, .gp-card-name, .gp-card-name-input")` 过滤交互元素，仅在点击卡片空白区时触发 `handleRefresh()`。修改 1 个文件（`index.vue`），零新增 lint 错误。

## GitPush 添加项目重复检测

**修改 2 个文件**：
- `useGitPush.ts`：`addProject()` 添加 `normalizePathForDedup()` 规范化路径比对，重复时 `throw Error`；`importScanResults()` 用 try-catch 包裹跳过重复项不阻塞整体导入
- `index.vue`：`handleAddFromDialog` catch 块从无声吞错改为 `alert(e?.message)`

零新增 lint 错误。

## GitPush 添加/导入目录选择后无反应修复

**根因**：AddProjectDialog 和 ScanImportDialog 各自有独立的本地 `ref` 保存路径，父组件通过 `pickDirectory` 获取的路径写入父组件 ref（`newProjectPath` / `scanDirInput`），但弹窗内部的本地 ref 未同步更新，导致输入框始终为空。

**修复**（3 个文件）：
- `AddProjectDialog.vue`：新增 `selectedPath` prop + `watch(() => props.selectedPath)` → 同步到本地 `path`
- `ScanImportDialog.vue`：新增 `watch(() => props.scanDir)` → 同步到 `localScanDir`
- `index.vue`：`AddProjectDialog` 传入 `:selected-path="newProjectPath"` prop

零新增 lint 错误。

## GitPush 推送/拉取输出可复制

在 `gp-output` 区域右上角添加悬停显示的复制按钮，点击调用 `copyToClipboard` 将输出文本写入剪贴板。修改 2 个文件：`index.vue`（模板 + `handleCopyOutput` 函数）、`styles/index.scss`（`.gp-output-copy` 样式）。零新增 lint 错误。

## GitPush index.vue 拆分重构

### 目标
将 2483 行的 index.vue 拆分为 3 个 composable + 7 个弹窗子组件，缩减至约 1200 行。

### 变更

**新增 composable（3 个）**：
- `composables/useTimeUtils.ts` — `relativeTime()`, `activityLevel()`, `sortProjects()` 纯工具函数
- `composables/useProjectFilters.ts` — `searchQuery`, `viewMode`, `showArchived`, `gitOpsPaused`, `selectedTags`, `smartViewProjects`, `filteredGroups`
- `composables/useIdeManagement.ts` — IDE_DETECTION + IDE_PRESETS + `scanIdes()`, `handleOpenIde()`, `launchIde()`, `isCmdAvailable()`, `resolvePath()`, `findSlnFile()`, `addCustomIde()`, `doRemoveCustomIde()`, `loadCustomIdes()`, `saveCustomIdes()` — ~230 行

**新增弹窗组件（7 个）**：
- `components/AddProjectDialog.vue` — 添加项目弹窗（props: i18n, categories; emit: add）
- `components/CategoryDialog.vue` — 分类管理弹窗（emit: add-category, delete-category）
- `components/SettingsDialog.vue` — 设置弹窗（props: concurrency; emit: save）
- `components/EditProjectDialog.vue` — 编辑项目弹窗（含远程仓库管理）（props: project, remoteList, allTags）
- `components/ScanImportDialog.vue` — 扫描导入弹窗（props: scanning, results, selection）
- `components/IdeManagementDialog.vue` — IDE 管理弹窗（props: customIdes, presetOptions）
- `components/RemoteConfigDialog.vue` — 远程仓库配置弹窗（emit: add-remote, remove-remote, edit-remote）

**跳过 ProjectCard 拆分**：30+ props + 25+ emits 的 prop drilling 反而降低可维护性，弹窗拆分 + composable 提取已充满足目标。

### 构建验证
- `npx vue-tsc --noEmit` 通过
- `npx vite build` 通过
- 零新增 lint 错误
- 通信模式：弹窗通过 props/emit 与 index.vue 解耦
- 所有 composable 内部使用 `getNodeProcessModules()` / `getNodeModules()` 不再通过 index.vue 传递

## SuperPanel 功能列表 2x2 网格布局

### 目标
将 superPanel 功能列表从单列纵向排列改为 2 列网格，卡片从水平行式改为纵向卡片式。

### 变更
- `styles/index.scss`: `.feature-list-inner` 从 `flex-direction: column` 改为 `display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px`，添加 `@include mobile-only` 窄屏回退单列
- `styles/feature-card.scss`: `.feature-card` 从 `display: flex; align-items: center` 改为 `flex-direction: column`，图标从 28px 增大至 36px 并 `align-self: flex-start`，描述改为 `-webkit-line-clamp: 2` 两行省略，`.feature-right` 添加 `margin-top: auto` 推至底部
- 纯 CSS 重构，零 Vue 模板/逻辑变更，TransitionGroup 动画、hover 效果、状态色均保持不变

## 修复 gitPush IDE 启动卡顿问题

### 根因
`useIdeManagement.ts` 中 `launchIde()` 使用 `cp.exec`（Windows 默认 `shell: true`），导致 IDE 通过 `cmd.exe` 间接启动，成为思源 Electron 的孙子进程，继承了 Electron 资源（文件句柄、GPU 上下文等）导致卡顿。手动直接打开 IDE 是独立顶级进程，无此问题。

### 修复
仅改 1 个文件 `src/features/gitPush/composables/useIdeManagement.ts`，4 处修改：

1. **`launchIde()`**：`cp.exec` → `cp.spawn(detached: true, stdio: 'ignore')`，使 IDE 脱离思源进程树
2. **`isCmdAvailable()`**：同步改为 `cp.spawn` + `error/close` 事件判断
3. **`findSlnFile()`**：`readdirSync` → `fs.promises.readdir` 异步，消除主线程阻塞
4. **`handleOpenCustomIde()`**：`findSlnFile` 调用加 `await`

构建验证通过，零 lint 错误。

## GitPush SCSS 审查与合并优化

### 发现的问题

1. **`.gp-status-badge` 命名冲突（Bug）**：同一文件中两处 `.gp-status-badge`（行 559 远程状态徽章 / 行 1185 项目状态按钮），CSS 级联导致第二个覆盖第一个的 `padding` 和 `display`，远程状态徽章丢失了 `padding: 1px 5px`。

2. **Label 样式 3 处完全重复**：`.gp-label` / `.gp-stash-label` / `.gp-tag-label` 共享 `font-size:10px; font-weight:700; letter-spacing:0.06em; text-transform:uppercase; opacity:0.45`

3. **Text-ellipsis 模式 10+ 处重复**：`white-space: nowrap; overflow: hidden; text-overflow: ellipsis` 在 `.gp-card-path` / `.gp-card-note` / `.gp-remote-url` / `.gp-scan-item-path` / `.gp-stash-msg` / `.gp-tag-name` / `.gp-tag-msg` / `.gp-ide-mgmt-path` / `.wt-file-path` 等中出现。

4. **`.gp-btn-danger` 散落在 index.scss**，应归入 `_buttons.scss` 模块

5. **`&::-webkit-scrollbar { height: 0; }` 重复**：`.gp-tabs` 和 `.gp-tag-filter`

### 修复

**修改 4 个文件，零新增 lint 错误**：

| 文件 | 变更 |
|------|------|
| `styles/_mixins.scss` | +3 mixin：`gp-label-base`、`text-ellipsis`、`hide-h-scrollbar` |
| `styles/_buttons.scss` | +`.gp-btn-danger`（从 index.scss 迁入） |
| `styles/index.scss` | 修复 `.gp-status-badge` 冲突→拆分 `.gp-project-status-btn`（+ `gp-psb-*` 修饰符）；10 处 `@include text-ellipsis`；3 处 `@include gp-label-base`；2 处 `@include hide-h-scrollbar`；移除 `.gp-btn-danger` |
| `styles/WorkingTreePanel.scss` | 1 处 `@include text-ellipsis`（`.wt-file-path`） |

### 模板同步修改

`index.vue` 第 210-211 行：`gp-status-badge` → `gp-project-status-btn`，`gp-sb-*` → `gp-psb-*`（与 SCSS 重命名对齐）

### 编译验证

- TypeScript/SCSS lint：零新增错误
- Vite build：通过

## imageCompressor 模块全面审查与优化

### 严重问题修复（3 处）
- **Blob URL 内存泄漏**：`scanner.ts` 中添加 `_blobUrl` 追踪 + `revokeImageUrls()` 工具函数，在重新扫描和替换图片时统一释放 blob URL
- **SQL 注入**：`index.vue` 中新增 `escapeSqlString()`，对 `navigateToDoc` 中拼接进 SQL 的路径和文件名做转义
- **原生 confirm**：替换为 i18n 键 `replaceConfirmMessage`（中英文）

### i18n 全覆盖
- 新增 15 个 i18n 键（zh_CN + en_US）：`replaceConfirmMessage`、`scanFilterLabel`、`displayFilterLabel`、`allOption`、`perPageOption`、`copyImageNameTitle`、`copyBtn`、`navigateToDocBtn`、`closePreviewTitle`、`clickToPreview`、`imageNameCopied`、`copyFailed`、`imageNotFound`、`loadFailed`、`pageInfo`
- 消除 `index.vue` 和 `CompressDialog.vue` 中所有硬编码中文 fallback

### CSS 修复
- 删除空 `&:hover img { }` 规则
- 3 处 `box-shadow` 改为 `border`（Codex UI 规范）
- 创建 `styles/CompressDialog.scss`，CompressDialog.vue 引用独立样式文件

### Composable 抽取
- 新增 4 个 composable：`useImageScanner`、`useImageSelection`、`useImagePagination`、`useImageCompress`
- index.vue 从 ~730 行精简至 ~600 行（script ~200 行 + template ~340 行）

### 次要修复
- 删除未使用的 `ImageComparison` 类型（types/index.ts + index.ts 导出）
- `copyImageName` 改用 `copyToClipboard` from `@/utils/domUtils`
- `onImageError` 改用 DOM API（`createElement` + `createElementNS`）替代 `innerHTML`
- 修复 `index.vue` 中重复的 `size="small"` 属性

### 验证
- ESLint：零错误
- i18n 验证（`pnpm i18n:verify`）：通过
- 架构校验（universal-arch-skill）：0 错误 0 警告
- 依赖完整性（code-explorer）：全部导入路径正确

## imageCompressor UI 尺寸缩小（与标准模块对齐）

### 审查发现
- 🔴 **Critical**：2 个 SCSS 文件缺失 `@use '@/variables.scss' as *;` 导入（imageCompressor 是 36 个模块中唯一未导入设计 Token 的模块）
- 🟠 **High**：12 处硬编码 `border-radius`（如 `4px`/`8px`/`12px`/`6px`）
- 🟠 **High**：35+ 处硬编码 `padding`/`gap`/`margin`
- 🟡 **Medium**：15+ 处硬编码 `font-size` + 2 处硬编码 `color: #fff`
- 🟢 **Low**：6 个交互元素缺少 `transition: 0.12s`
- ✅ `box-shadow` 已全部合规（0 处违规）

### 第一次修复（出错）
- 使用 `$spacing-xs/sm/md/lg` 和 `$vp-radius` → 构建失败：`Undefined variable`
- **根因**：`@/variables.scss` 只有 `$spacing-1`~`$spacing-16`（数字后缀），`$spacing-xs/sm/md/lg` 是 `superPanel`/`passwordVault` 的本地别名。CLAUDE_RULES.md 的 Token 表存在误导（11 个模块实际使用数字后缀命名）。

### 第二次修复（正确）
- `$spacing-xs`→`$spacing-1`、`$spacing-sm`→`$spacing-2`、`$spacing-md`→`$spacing-3`、`$spacing-lg`→`$spacing-4`
- `$vp-radius: 6px` 在 `index.scss` 和 `CompressDialog.scss` 中本地声明
- 保留 `$spacing-5`(20px)/`$spacing-6`(24px) 等数字后缀（原本就存在于 `@/variables.scss`）
- 构建验证通过，零残留检测通过

### 第三次修复（尺寸缩小对齐标准模块）
- **背景**：用户反馈 UI 明显偏大，与其他功能差距明显
- **对照**：与 superPanel/rssReader/resourceManager 3 个模块逐属性对比
- **变更**（index.scss + CompressDialog.scss）：
  - 标题 font-size：20px→16px（`$font-size-base`）
  - Header padding：16px/24px→12px/16px（`$spacing-3/$spacing-4`）
  - 工具栏 gap：12px→8px（`$spacing-2`），padding：24px→16px（`$spacing-4`）
  - Badge `.image-count`：font-size 13px→12px（`$font-size-xs`），padding 4px/12px→2px/8px
  - 列表网格 gap：16px→12px（`$spacing-3`），padding：16px/24px→12px/16px
  - 卡片 min-height：300px→260px，预览高度：160px→140px
  - 图标按钮：从 `padding:8px` 改为固定 26×26px+无 padding（对齐 rssReader 模式），icon 20px→16px
  - 进度条高度：24px→20px
  - 弹窗 padding：20px→16px
  - 结果区 padding：24px→16px
  - filter-group 宽度：180px→160px
- 验证：ESLint 零错误，Vite build 通过

## CLAUDE_RULES.md Codex 规则修正

基于 superPanel 实际实现修正了存在严重误导的 Token 表：

### 根因
原规则误导声称 `$vp-radius`、`$spacing-xs/sm/md/lg`、`$vp-mono` 来自 `@/variables.scss`，实际全部不存在——导致 imageCompressor 出现 `Undefined variable` 编译错误。

### 修正内容
| 项目 | 原（错误） | 修正（与 superPanel 对齐） |
|------|-----------|--------------------------|
| Token 表 | 列出不存在的变量名 | 准确列出 `@/variables.scss` 实际变量 + 分离"需本地声明"段落 |
| 间距命名 | `$spacing-xs/sm/md/lg` | `$spacing-1/2/3/4`（数字后缀是全局标准） |
| `$vp-radius` | 声称全局可用 | 标记为"需本地声明"（存在于 superPanel 本地 variables.scss） |
| `$vp-mono` | 声称全局可用 | 标记为"需本地声明" |
| 代码示例 | 全部使用 `$spacing-lg` 等错误变量名 | 改为 `$spacing-4` 等正确变量名 |
| 禁止事项 | 说禁止各自定义 `$vp-radius/$spacing-sm` | 改为：禁止使用 `$spacing-xs/sm/md/lg`（这些不是全局变量） |
| 新增项 | — | 图标按钮固定尺寸规范、标题 font-size ≤16px 规范 |

### 文件
- CLAUDE_RULES.md：重写 Token 表 + 所有代码示例 + 禁止事项表
- CODEBUDDY.md：同步修正 SCSS 规范摘要

## Codex UI 参考页面

创建了 `docs/codex-ui-reference.html`，一个可浏览器打开的完整 UI 组件参考页面，包含：
- 设计 Token 表（圆角/间距/字号，含实际来源标注）
- 12 个组件 Showcase：弹窗、卡片（含选中/hover 态）、4 种按钮变体、图标按钮、Badge/Tag/Uppercase Label、表单输入（含等宽变体）、头部/工具栏、统计卡片/行式统计、进度条、空状态、分割线、hover hint
- 间距可视化条（sp-1 到 sp-6 实际像素对比）
- 每个组件附 Codex 规范代码块
- 快速使用指南（新建 feature 的标准 SCSS 模板）
- 纯粹 HTML + 内联 CSS，无需构建，直接浏览器打开即可查看

## 图片压缩集成到状态栏

在 statusBar 的 FEATURES 注册表中添加 imageCompressor 条目：
- **抽屉图标**：`mdi:image`（红色 `#ef4444`）
- **状态栏快捷图标**：`ph:image`
- **触发方式**：`emitCustomEvent("openImageCompressor")`（与现有命令一致，dock 面板自动响应）
- **样式**：statusBar `styles/index.scss` 新增 `.image-compressor-item { color: #ef4444; }`
- **pinnable**：true（可固定到状态栏 / 标记不常用）
- 验证：`pnpm validate:icons` 通过 + lint 零错误 + build 通过

## statusBar Codex UI 样式修复

### 审查发现
- 🔴 `.feature-drawer` 存在 `box-shadow`（Codex 禁止）
- 🟠 缺失 `@use '@/variables.scss'`，用本地 `$sb-radius`/`$sb-mono` 替代全局 Token
- 🟠 FeatureDrawer 内 40+ 处硬编码 `border-radius`/`padding`/`gap`
- 🟡 8 处 `transition: 0.15s` 应统一为 `0.12s`
- 🟢 状态栏本体（29px高度/11px字体/2-3px gap）合理豁免

### 修复
- `styles/index.scss`：
  - L1：`@use '@/variables.scss' as *;` + 本地 `$vp-radius: 6px; $vp-mono: ...;`
  - 移除 `.feature-drawer` 的 `box-shadow` → 仅保留 `border`
  - `$sb-mono` → `$vp-mono`，`$sb-radius` → `$vp-radius`/`$radius-lg`
  - `border-radius`：`4px`→`$radius-sm`，`6px`→`$vp-radius`，`12px`→`$radius-lg`
  - `padding`/`gap`：`4px`→`$spacing-1`，`6-8px`→`$spacing-2`，`10-14px`→`$spacing-3`
  - `font-size`：`12px`→`$font-size-xs`，`13px`→`$font-size-sm`
  - 8 处 `transition: 0.15s` → `0.12s`
- 验证：Lint 零错误 + build 通过 + 零 `box-shadow`/`0.15s`/`$sb-*` 残留

## $vp-radius / $vp-mono 全局化

### 用户建议
不应各模块重复声明，应在 `_variables.scss` 直接定义。

### 变更
- `src/_variables.scss`：新增 `$vp-radius: $radius-base;` 和 `$vp-mono` 全局定义
- 清理 9 个 SCSS 文件的本地声明（imageCompressor×2, statusBar, floatingBox, encryption, everythingSearch＋补充 `@use`, generalSettings×2, gitPush→`!default`）
- superPanel/passwordVault 保留 `!default`（权威定义源，自动兼容）
- CLAUDE_RULES.md：Token 段改为"全局可用"，新增禁止重复声明行

## CODEBUDDY.md 创建

基于 CLAUDE.md、CLAUDE_RULES.md、README.md 及源代码综合分析，创建了 CODEBUDDY.md 供未来的 CodeBuddy Agent 工作使用。覆盖：命令参考（dev/build/lint/i18n/发布）、架构概述（启动流程、Feature 模块结构、8 步注册清单、编译时完整性断言）、跨功能通信（EventBus + App.vue 调度）、统一入口点、持久化 Modal 模式、设置架构（flags + settings 双层）、i18n 分片架构、SCSS 规范、构建流程、图标系统。全文翻译为中文。后续添加了 CLAUDE_RULES.md 交叉引用（统一入口点→API 参考、持久化 Modal→实现步骤、SCSS→Codex 规范/分离规则），并在末尾新增"参考文件"章节列出 CLAUDE_RULES.md 的完整章节索引。
