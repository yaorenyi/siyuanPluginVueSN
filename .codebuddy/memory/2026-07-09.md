# 2026-07-09

## statusBar：监控项与功能快捷入口视觉分隔

- 在 `index.vue` 模板中，于最后一个监控项（monitor-uptime）之后、动态任务/快捷入口之前插入 `<span class="status-bar-separator">` 分隔线
- 新增 `showSeparator` computed：仅当 `visibleMonitors.size > 0` 且（`statusBarShortcuts.length > 0` 或 `activeTasks.length > 0`）时显示
- 在 `styles/index.scss` 新增 `.status-bar-separator` 样式：1px 宽、18px 高竖线，颜色 `--b3-theme-border`，`align-self: center`

## statusBar：FeatureDrawer "全部"Tab 隐藏监控项

- 在 `FeatureDrawer.vue` 的 `displayItems` computed 中，"全部"Tab（`activeGroup === "__all__"`）过滤掉 `group === "监控"` 的条目
- 搜索模式不受影响，"监控"独立 Tab 仍正常显示所有监控项

## statusBar：代码审查与重构（6 项改造）

审查范围：9 个 statusBar 模块文件 + 2 个关联文件。共修复 16 个问题。

**冗余/重复代码（5处）**：
- R1：FeatureDrawer.vue 主列表/不常用列表 `feature-drawer-item` 渲染块 ~40 行重复 → 提取 `DrawerFeatureItem.vue` 子组件复用
- R2：FeatureDrawer.vue 关闭/清除 SVG path 重复 → 替换为 `<Icon icon="ph:x">`
- R3：index.vue `handleToggleStatusBar` 与 `handleToggleRarelyUsed` 重复 save 模式 → 提取 `saveCategory()` 辅助函数
- R4：useStatusBarTask.ts `complete()` 与 `fail()` 相同 4 步模式 → 提取 `finish()` 私有函数
- R5：useStatusBar.ts `totalWordsDisplay` 重复创建阈值数组 → 提取 `WORD_COUNT_THRESHOLDS` 常量

**边界异常（3处）**：
- E1：index.ts `unregisterStatusBar()` 死代码 → 导出并在 `src/index.ts` `onunload()` 中调用，修复插件卸载内存泄漏
- E2：FeatureDrawer.vue 关闭不清空搜索/分组 → 添加 `watch(visible)` 重置
- E3：useStatusBar.ts `uptimeSeconds` 每 3 秒无条件更新 → 添加分钟级阈值过滤

**性能优化（2处）**：
- FeatureDrawer.vue `matchSearch` 每次 `toLowerCase()` → 缓存 `searchQueryLower` computed
- useStatusBar.ts `WORD_COUNT_THRESHOLDS` 常量提取（与 R5 合并）

**规范合规（4处）**：
- 为 index.vue、FeatureDrawer.vue、MonitorItem.vue、DrawerFeatureItem.vue、useStatusBar.ts、useDataSnapshot.ts 添加文件头注释
- index.scss 3 处 `font-size: 11px` → `$font-size-xs`

**修改文件清单**：
- 新建：`components/DrawerFeatureItem.vue`
- 修改：`index.ts`、`index.vue`、`FeatureDrawer.vue`、`useStatusBarTask.ts`、`useStatusBar.ts`、`styles/index.scss`、`useDataSnapshot.ts`
- 集成：`features/index.ts`（导出 unregisterStatusBar）、`src/index.ts`（import + onunload 调用）

## statusBar：SCSS 深度审查与重构

仅改 `styles/index.scss`（503→510 行）。消除 15 处冗余/性能问题。

**基础设施新增（+26 行）**：
- 本地变量：`$_gap-xs: 3px`、`$_pad-tab-y: 3px`、`$_pad-tab-x: 10px`、`$_grid-title-size: 0.625rem`
- 占位选择器：`%flex-center`（display:flex+align-items:center）、`%flex-shrink-0`
- Mixin：`hover-lift($rest, $hover)` — 替换 4 处重复的 opacity hover 模式
- 缓动变量：`$_ease-out: cubic-bezier(0.4, 0, 0.2, 1)`

**冗余消除（10 处）**：
- `%flex-center` 替换 12 处 `display:flex;align-items:center`
- `%flex-shrink-0` 替换 10 处 `flex-shrink:0`
- `hover-lift` mixin 替换 4 处 `opacity:X; &:hover{opacity:1}`（statistics/search-clear/rarely-header/action-item）
- 6 处硬编码色值提取为 `--status-color-*` CSS 自定义属性
- 2 处重复 `cubic-bezier(…, 0.2, 1)` 替换为 `$_ease-out`
- data-level 图标规则合并到颜色块（消除冗余选择器重复）
- `.rarely-list` 作用域嵌套到 `.feature-drawer-rarely` 下

**性能优化（2 处）**：
- `.feature-drawer-view-btn/.close` transition 移除多余 `opacity 0.12s`
- `.feature-drawer-tab` transition 移除多余 `border-color 0.12s`

**规范合规（3 处）**：
- `gap:3px` → `$_gap-xs`（grid-mode）
- `font-size:10px` → `$_grid-title-size`（grid-mode）
- `padding:3px 10px` → `$_pad-tab-y $_pad-tab-x`

## statusBar：微间距变量提升为全局 Token

将 statusBar SCSS 中的本地 3px 微间距变量 `$_gap-xs` / `$_pad-tab-y` 提升为全局 `$spacing-px: 3px`，纳入 `_variables.scss` 间距体系。

**改动文件（4 个）**：
- `src/_variables.scss`：间距区新增 `$spacing-px: 3px`（排在 `$spacing-0` 之前），附带注释说明密集布局专用
- `src/features/statusBar/styles/index.scss`：移除 `$_gap-xs: 3px` 和 `$_pad-tab-y: 3px` 两个本地变量声明；`$_gap-xs` → `$spacing-px`（grid-mode gap），`$_pad-tab-y` → `$spacing-px`（tab padding）；保留 `$_pad-tab-x` / `$_grid-title-size` / `$_ease-out` 本地变量不变
- `CLAUDE_RULES.md`：间距表新增 `$spacing-px: 3px` 行（排在 `$spacing-1` 之前）
- `CODEBUDDY.md`：设计 Token 列表补充 `$spacing-px`

## 全局间距：补充 $spacing-2px

在 `_variables.scss` / `CLAUDE_RULES.md` / `CODEBUDDY.md` 三处同步新增 `$spacing-2px: 2px`（超微间距，极窄分隔线/图标紧贴场景）。与 `$spacing-px: 3px` 并列排在 `$spacing-0` 之前。

## flashcardReading：全面代码审查与重构（7 步，28 文件）

### 1. 文件头注释补充（13 个文件）
为 `types/storage.ts`（注释移顶）、`composables/useFlashcardStorage.ts`、`usePlayWord.ts`、`useTypingQueue.ts`、以及 8 个 .vue 组件补全文件头注释。

### 2. SCSS Token 规范化（5 个文件）
- `_variables.scss`：删除本地 `$fc-mono`/`$fc-radius` 声明（用全局 `$vp-mono`/`$vp-radius` 替代）
- `index.scss`、`_dialog.scss`、`_typing.scss`、`_statistics.scss`：所有 `$fc-mono`→`$vp-mono`、`$fc-radius`→`$vp-radius`；硬编码 `font-size: 12px/14px/15px/18px/20px`→`$font-size-xs/sm/base/lg/xl`；硬编码 `padding: 4px/8px/12px/16px`→`$spacing-1/2/3/4`；硬编码 `border-radius: 4px/12px`→`$radius-sm/lg`
- 提取 `FlashcardDialog.vue` 内联样式 → 新建 `styles/FlashcardDialog.scss`

### 3. 表单组件规范修复
- `CardDialog.vue`：4 个 Input/Select 添加 `size="small"`
- `FlashcardDialog.vue`：Select 添加 `size="small"`

### 4. Emoji 图标替换（TypingPractice.vue）
- `↻`→`IconWrapper name="refresh"`、`◌/◉`→`eyeOff/eye`、`⏱`→`timerOutline`（共 5 处）
- 新增 `typing-case-toggle__icon` / `typing-timer__icon` SCSS 类

### 5. 打字队列逻辑修复
- `useTypingQueue.next()`：移除 `rebuild()` 调用（每卡前进不再 O(n²) 重建，改为 O(1) index++）
- `useTypingQueue.random()`：移除 `rebuild()`，从 queue 而非 cards 中随机选（修复与 filteredCards 不一致的潜在 bug）
- 移除未使用的 `weightedRandomIndex()` 函数
- `index.vue watch([searchQuery, selectedCategory])`：添加 `typingQueue.rebuild()` 显式重建

### 6. 边界 Bug 修复
- `FlashcardDialog.vue previousCard/nextCard`：将 `playWord` 移入 `if` 分支，避免首尾边界处重复播放同一张卡
- `incrementPracticeCount` 竞态条件评估为低风险（单进程顺序执行），未添加乐观锁

### 7. 重复代码提取（新建 utils.ts）
- 新建 `utils.ts`：`syncIncrementPractice(storage, cards, cardId)` — 消除 usePlayWord 与 index.vue onTypingCorrect 中完全重复的 5 行 increment 逻辑
- 新建 `copyAndNotify(text, successMsg, errorMsg?)` — 消除 FlashcardDialog 与 index.vue 中 handleCopy 重复逻辑
- `FlashcardDialog.vue`：移除未使用的 `showMessage` import

**修改文件清单（18 个）**：
- 新建：`utils.ts`、`styles/FlashcardDialog.scss`
- 修改：`index.vue`、`FlashcardReading.ts`、`index.ts`（无变更）、`types/storage.ts`、`types/index.ts`（无变更）
- 4 composables：`useFlashcardStorage.ts`、`usePlayWord.ts`、`useTypingQueue.ts`、`useI18n.ts`（无变更但头注释）
- 6 components：`CardDialog.vue`、`CardList.vue`、`CategoryFilter.vue`、`FlashcardDialog.vue`、`PanelHeader.vue`、`SingleCardView.vue`、`StatisticsView.vue`、`TypingPractice.vue`
- 5 styles：`_variables.scss`、`index.scss`、`_dialog.scss`、`_typing.scss`、`_statistics.scss`

## flashcardReading：删除单卡视图模式

从 ViewMode 联合类型移除 `"single"`，涉及 7 个文件：
- `types/index.ts`：ViewMode 移除 `"single"`（已在之前完成），I18n 接口移除 `singleView?`
- `index.vue`：移除 SingleCardView 组件引用（v-else-if viewMode==='single' 块）和单卡视图切换按钮，`switchMode` 参数改为仅 `"typing"`
- `useI18n.ts`：DEFAULT_I18N 移除 `singleView`
- `i18n/zh_CN/flashcardReading.json`：移除 `singleView`、`helpSingleView`
- `i18n/en_US/flashcardReading.json`：移除 `singleView`、`helpSingleView`
- `i18n/zh_CN.json`：移除 `singleView`、`helpSingleView`
- `i18n/en_US.json`：移除 `singleView`、`helpSingleView`

保留不动：`SingleCardView.vue`（FlashcardDialog 共用）、`navigateAndPlay` 函数（TypingPractice 仍用）。

## flashcardReading：清理单卡视图冗余样式

删除 `index.scss` 中嵌套在 `.flashcard-reading-panel` 下的死样式（单卡视图已从主面板移除，FlashcardDialog 无此 class 故不可达）：
- 删除 `.single-card-view` 容器块（265-273）
- 删除 `.flashcard-item--single` 中的 `.card-title--single`（仅 SingleCardView 用）和 `.card-actions` hover 规则
- `.single-card-view, .typing-practice { > .flashcard-item {...} }` → 仅保留 `.typing-practice`

**新建 `styles/SingleCardView.scss`**：提取 SingleCardView 独有样式（容器布局、card-title--single 大字标题、card-actions hover），不嵌套在 `.flashcard-reading-panel` 下，使 FlashcardDialog 也能正确渲染。

**补充样式引用**：
- `SingleCardView.vue`：新增 `<style scoped>` 导入 `SingleCardView.scss`
- `FlashcardDialog.vue`：补充 `@use '../styles/SingleCardView.scss'`

共享样式保留：`.flashcard-item--single` 中的 `.card-title--covered` 和 `.card-footer--single`（TypingPractice 仍使用）。
