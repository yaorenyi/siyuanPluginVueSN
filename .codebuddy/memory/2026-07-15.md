# 2026-07-15

## 移除 aiContentGenerator 提示词功能

全面移除了 `aiContentGenerator` 模块中的提示词（Prompts）功能。变更摘要：

### 删除（3 个文件 + 类型/工具函数）
- `composables/usePromptManager.ts` — 提示词管理 composable（~180行）
- `components/SettingsPanel.vue` — 提示词配置对话框（~232行）
- `styles/SettingsPanel.scss` — SettingsPanel 专属样式（~200行）

### 修改（8 个源文件 + 4 个 i18n 文件）
- `index.vue` — 移除 SettingsPanel/usePromptManager 导入和引用、systemPrompt/temperature/maxTokens ref、currentPromptName、showSettings/toggleSettings、saveSettings 中提示词字段、BottomInputArea 的提示词 props/events
- `BottomInputArea.vue` — 移除提示词选择器模板（~60行）、4 个 prompt props、5 个 prompt emits、getOriginalIndex 函数、SavedPrompt/getPromptPreview 导入
- `PanelHeader.vue` — 移除设置按钮和 emits
- `useGeneration.ts` — systemPrompt/temperature/maxTokens 从外部 Ref 改为内部硬编码常量（DEFAULT_TEMPERATURE=0.7, DEFAULT_MAX_TOKENS=10000）
- `types/storage.ts` — 移除 SavedPrompt 导入/导出、prompts/currentPrompt TypedStorage 字段、AISettings 中 systemPrompt/temperature/maxTokens
- `utils.ts` — 移除 getPromptPreview 函数
- `src/types/ai.ts` — 移除 SavedPrompt 接口
- `styles/index.scss` — 移除 ~60 行提示词面板样式（prompt-selector-*、prompt-list、prompt-item-*、badge、dropdown-overlay）
- i18n 4 个文件 — 移除 7 个提示词专用键（selectPrompt/savedPrompts/noSavedPrompts/savePromptConfig/promptNamePlaceholder/enterPromptName/currentPrompt），保留 confirmDelete（共享键）

### 审查发现
- 无跨文件残留引用
- useGeneration.ts: DEFAULT_SYSTEM_PROMPT 常量已移除（由调用方传入 systemPrompt 字符串）
- index.vue 和 ReviewPanel.vue 有预存在的 lint 问题，非本次引入

## 二次审查：清理冗余代码

移除提示词功能后继续审查，发现并修复 7 处冗余：

### useGeneration.ts
- 从 return 对象移除 4 个内部使用的导出：`abortController`、`currentProvider`、`startGeneration`、`resetAllGenerationStates`

### index.vue
- 移除未使用的 `loadTargetDocument` 解构（-1 warning）
- 移除 7 处无意义的章节编号（`// 1.`/`// 4.`/`// 5.`/`// 6.`/`// 2.`）

### BottomInputArea.vue
- 修正残留注释：`文档选择 + 技能 + 提示词` → `文档选择 + 技能 + RAG`
- 移除 `</script>` 前多余空行

### PanelHeader.vue + PanelHeader.scss
- 移除空 `<script setup lang="ts">` 块
- 移除 `.header-actions` 死样式 + `.panel-header` 无用的 `justify-content: space-between`
