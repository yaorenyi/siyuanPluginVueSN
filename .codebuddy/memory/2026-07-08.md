## 2026-07-08

### Git 全局配置查询功能
- 在 gitPush 模块的 PanelHeader 中新增了"本机 Git 配置查询"按钮（`mdi:information-outline` 图标）
- 新增 `GitPushManager.getGitGlobalConfig()` 方法，执行 `git config --global --list` 获取全局配置
- 新增 `GitConfigDialog.vue` 弹窗组件（参照 MarkdownPreviewDialog 模式），支持加载/错误/空/正常四种状态展示
- 新增 `GitConfigDialog.scss` 样式，等宽字体展示 key=value 配置项
- 底部操作栏支持"复制全部"到剪贴板功能
- 中英文 i18n 已同步（gitConfigTitle/gitConfigLoading/gitConfigEmpty/gitConfigCopyAll）
- lint 通过，0 error
- 后续新增：为 GitConfigDialog 新增 80+ 个 Git 配置键的中文说明映射（GIT_CONFIG_DESC），支持通配匹配（alias.*/remote.*.url/filter.* 等），中文说明以半透明括号形式显示在 key 和 value 之间
- 后续新增：GitConfigDialog 底部增加"打开配置文件"按钮（`shell.openPath` 打开 `~/.gitconfig`）
- 后续新增：ProjectCard 操作栏增加项目 Git 配置查看按钮（`mdi:file-document-outline`），复用 GitConfigDialog 弹窗展示项目级 `git config --local --list` 输出，GitPushManager 新增 `getProjectGitConfig()` 和 `getProjectGitConfigFilePath()` 方法

### Markdown 组件审查与重构
- 审查 `MarkdownPreviewDialog.vue` 和 `MarkdownFileBadge.vue`，发现并修复 7 个问题：
  1. 删除本地重复的 `escapeHtml` 函数，改用 `@/utils/stringUtils.ts` 共享版本（更完整，额外转义单引号）
  2. `marked.setOptions()` 从 computed 内部提升到模块级别，只执行一次
  3. 添加 `onUnmounted` 清除 copied 反馈的 setTimeout 定时器
  4. 移除 `loadFile` 中永远不会触发的 try-catch 死代码（`readMarkdownFile` 内部已捕获所有异常）
  5. SCSS `font-weight: 500` → `$font-weight-medium`
  6. SCSS `font-size: 14px` → `$font-size-sm`
  7. SCSS `font-weight: 600` → `$font-weight-semibold`
- Lint 通过，0 error。所有改动不影响现有功能逻辑。

### Markdown 渲染统一入口重构
- 新建 `src/utils/mdRenderer.ts` 作为项目唯一 Markdown 渲染入口，消除 3 套独立实现的代码重复
- 导出 `HLJS_INLINE_COLORS`（38 条映射）、`convertHljsToInlineStyles()`、`parseMarkdown(mdText, options?)`（支持 codeHighlight/inlineStyles/breaks/gfm 配置）
- 按 `"highlight"` / `"highlight-inline"` key 缓存最多 2 个 Renderer 实例，无高亮时不缓存
- 修改涉及 6 个文件（1 新建 + 5 修改）：
  1. **新建** `src/utils/mdRenderer.ts` — 统一渲染模块 ~130 行
  2. **修改** `docAnalysis/utils/mdRenderer.ts` — 改为重新导出共享模块（向后兼容，PublishPanel.vue 无需改动）
  3. **修改** `formatAssistant/utils/mdToShared.ts` — 移除私有 HLJS_INLINE_COLORS + convertHljsToInlineStyles（~58 行），导入共享模块
  4. **修改** `gitPush/components/MarkdownPreviewDialog.vue` — 删除内联 marked/Renderer/hljs 实现（~55 行），改用 `parseMarkdown({ codeHighlight: true })`
  5. **修改** `aiContentGenerator/utils.ts` — 移除 marked import + 全局 setOptions，改用 parseMarkdown
  6. **修改** `skillsViewer/index.vue` — 移除 marked import + 内联 renderMarkdown，改用 parseMarkdown
- 净减少约 170 行重复代码，全部 6 个文件 lint 零错误

### 规则文件更新：Markdown 渲染统一入口
- 在三个规则文件中同步添加了 `parseMarkdown` / `convertHljsToInlineStyles` 作为强制统一入口：
  1. **CODEBUDDY.md** — "统一入口点"表格新增一行：`Markdown 渲染 | parseMarkdown | @/utils/mdRenderer`
  2. **CLAUDE.md** — "统一入口原则"表格新增：禁止各自 renderer，改用 `@/utils/mdRenderer`
  3. **CLAUDE.md** — "关键文件速查"新增 `mdRenderer.ts` 条目
  4. **CLAUDE_RULES.md** — 新增"Markdown 渲染"小节（含完整 API 示例），放在"状态栏后台任务"之前

### mdRenderer.ts 修复与增强（5 项）
- 对 `src/utils/mdRenderer.ts` 进行了审查修复和功能增强：
  1. **修复全局状态污染**：`marked.setOptions({ breaks, gfm })` 改为 `marked.parse(mdText, { breaks, gfm, ... })` 按调用传参
  2. **修复 lang 属性注入风险**：`class="language-${lang}"` → `class="language-${escapeHtml(lang)}"`
  3. **导出 `clearRendererCache()`**：清除缓存的 Renderer 实例，在 `src/index.ts` 的 `onunload()` 中调用
  4. **新增 `extensions` 字段**：`ParseMarkdownOptions` 增加 `extensions?: TokenizerAndRendererExtension[]` 透传给 marked
  5. **新增命名预设**：支持 `"basic"` / `"highlight"` / `"wechat"` 三个字符串预设；`parseMarkdown` 签名扩展为 `(mdText, options?: ParseMarkdownOptions | MarkdownPreset)`
- `src/features/docAnalysis/utils/mdRenderer.ts` 外壳简化为 `sharedParseMarkdown(mdText, "wechat")`
- 所有向后兼容，现有调用方无需修改

### shortcut 模块全面代码审查与重构
- 对 `src/features/shortcut/` 模块进行了 5 大类审查与优化，涉及 9 个文件修改 + 1 个新建 + 2 个 i18n 文件更新：

**逻辑漏洞修复（3 处）**：
1. `confirmDelete()` 补充 `recentUsed` 持久化 + 检查 `removeShortcut` 返回值（失败时跳过本地清理）
2. `manager.ts` 的 `search()` 添加 `!keyword` null 防护快速返回
3. `STORAGE_KEYS` 数组补全 `SHORTCUTS_ALL_KEY`

**冗余代码消除（4 处）**：
1. `manager.ts` 提取 `_upsertOne()` 私有方法，`addShortcut`/`addShortcuts` 复用（消除 ~20 行重复）
2. `storage.ts` 提取 `sanitizeStringArray()` 模块级纯函数，替代 4 处重复的类型校验+过滤
3. 移除 `ShortcutManagerConfig` 未使用的接口
4. 移除 `manager.ts` 构造函数冗余赋值 `this.shortcuts = []`

**性能优化**：
- `recentUsed` 从 `string[]`（O(n) 查找）升级为 `Map<string, number>`（O(1) 查找），关联更新 `isRecent`/`addToRecent`/`confirmDelete`/`filteredShortcuts` 中 5 处引用点

**编码规范修正（8 处）**：
1. 5 个 .vue 组件补全文件头注释（index.vue/PanelHeader/ShortcutCard/ShortcutDialog/ShortcutGrid）
2. `index.vue` 删除确认弹窗 47 行内联 SCSS 提取到新建 `styles/DeleteConfirmDialog.scss`
3. `ShortcutDialog.vue` 4 个 `<Input>` 添加 `size="small"`（Codex UI 规范）
4. i18n：zh_CN/en_US 各新增 3 个筛选标签键（filterAll/filterFavorite/filterRecent）

**数据同步维护性**：
- `TOOL_CATEGORIES` 和 `CATEGORY_LABEL_I18N_KEYS` 提取到 `types/index.ts`，`index.vue` 从 types 统一导入
- `QUICK_FILTERS` 标签接入 i18n
- `index.ts` 导出新增的常量（向后兼容）

### S3 备份列表时间格式化
- S3 云端备份列表的 `lastModified` 字段原样显示 ISO 字符串（`2026-07-06T16:19:23.685Z`），不友好
- 在 `src/utils/format.ts` 新增 `formatTime()` 函数，将 ISO 字符串/时间戳/Date 转为 `YYYY-MM-DD HH:mm:ss` 格式
- 修改 `BackupListCard.vue`：导入 `formatTime`，模板中 `item[timeKey]` → `formatTime(item[timeKey])`
- lint 通过，本地备份列表的时间（`toLocaleString()` 格式）也经此格式化，统一为 `YYYY-MM-DD HH:mm:ss`

### S3 自动备份重启误触发修复（B18）
- 根因：`startAutoBackupTimer()` 中 `case "daily"` 分支缺失 `timeSinceTimerStart >= 60 * 1000` 守卫
- 跨日重启场景：昨天 03:00 正常备份 → 今天 10:56 重开程序 → B17 无法防止（只防同日）→ 第一 tick 三个条件全满足 → 立即触发备份
- 修复：在 daily 分支 if 条件中增加 `timeSinceTimerStart >= 60 * 1000`（与 minute/hourly 模式对齐），并更新注释标注 B18 修复
- 仅修改 `src/features/s3Backup/index.ts` 1 个文件，lint 通过
