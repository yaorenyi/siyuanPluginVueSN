# 2026-06-17

## GitPush 新增 CNB (cnb.cool) 托管平台支持

### 需求
在现有 GitHub/Gitee/Gitea 三平台基础上，新增 CNB（cnb.cool）代码托管平台支持。

### 审查发现
- PLATFORM_META 驱动所有平台逻辑（检测/推送/拉取/统计/UI），新增平台只需扩展此数据源
- `detectRemotes` 按 URL 域名 + remote 名称双重匹配（不区分大小写）
- Gitea 作为兜底分类（所有非 GitHub/Gitee/CNB 的平台归入 Gitea）
- `pushSingle`/`pullSingle` 硬编码了三元表达式远程名回退，需新增 CNB 分支
- `remoteCoverage`/`needsPushProjects`/`platformStatusProjects` 都硬编码了平台字段

### 修改文件（6 个）

**`types/storage.ts`**：
- `GitProject` 接口新增 `cnbRemote?: string` / `cnbUrl?: string`
- `GitRemoteInfo` 接口新增 `isCnb: boolean`

**`types/index.ts`**：
- `PLATFORM_META` 新增 CNB 条目（key: "cnb", icon: "mdi:cloud-braces", label: "CNB"）；添加 `as const` 确保 literal 类型；导出 `PlatformKey` 类型
- `applyRemotesToProject`：新增 cnbRemote/cnbUrl 清除和赋值
- `detectRemotes`：新增 `isCnb` 检测（`lowerUrl.includes("cnb.cool") || lowerName === "cnb"`）；Gitea 兜底改为 `!isGithub && !isGitee && !isCnb`
- `isKnownRemote`：新增 `"cnb"` 分支；参数类型改为 `PlatformKey`
- `pushToAll`/`pullToAll`：返回值新增 `cnb` 字段；success 判断包含 `cnb.ok`
- `pushSingle`/`pullSingle`：目标类型改为 `PlatformKey`；新增 `project.cnbRemote || "cnb"` 回退
- `checkCanPushToCloud`：返回值新增 `cnb: boolean`
- `updateProjectMeta` Pick 类型新增 `"cnbUrl"`

**`composables/useGitPush.ts`**：
- 导入 `PlatformKey`
- `remoteCoverage`：新增 `cnb` 字段；remotes 计数数组新增 `p.cnbUrl`；`noRemote` 初始值新增 `cnb: 0`
- `needsPushProjects`：3 个 if 分支 → `for...of PLATFORM_META` OCP 循环（自动包含 CNB）
- `platformStatusProjects`：新增 `cnb: boolean` 字段；missingCount 参与计算；`noPlatformProjects` 的 missingCount 比较改为 `PLATFORM_META.length` 动态计算
- `pushSingle`/`pullSingle`：target 类型改为 `PlatformKey`

**`index.vue`**：
- 移除 push/pull 的 `as 'github' | 'gitee' | 'gitea'` 类型断言（`REMOTES` 从 `PLATFORM_META.map()` 派生，类型自动推断）

**`components/StatsPanel.vue`**：
- `RemoteCoverage` 接口新增 `cnb: number`
- `PlatformStatusItem` 接口新增 `cnb: boolean`
- 覆盖率区域新增 CNB 行（蓝色进度条，`gp-coverage-fill--cnb`）

**`styles/StatsPanel.scss`**：
- 新增 `.gp-coverage-fill--cnb { background: #00a8e8; }`

### 架构要点
- `checkPushStatus` 无需改动：内部用 `for...of PLATFORM_META` 遍历（之前已重构为 OCP）
- `isPushing`/`isPulling` 无需改动：参数对比用 `string` 类型
- TagPanel 无需改动：`remotes` prop 从 `index.vue` 动态传入
- 平台检测：CNB 优先级在 Gitea 之前（`isCnb` 先判断，`isGitea` 改为 `!isGithub && !isGitee && !isCnb`）

### 构建验证
- `npx vue-tsc --noEmit` 通过（零类型错误）
- `npx vite build` 通过（零编译错误）
- 零新增 lint 错误（仅 1 个预存 deprecated hint: `noPlatformProjects`）

## GitPush 仓库链接右键复制

### 修改（1 个文件）
- **`index.vue`**：GitHub/Gitee/Gitea 按钮添加 `@contextmenu.prevent="handleCopyUrl(url)"` 右键复制；tooltip 更新为"打开 XXX（右键复制链接）"；新增 `handleCopyUrl()` 函数（调用 `copyToClipboard` + 临时 tooltip "已复制链接 ✓" 1.5s 后恢复）

### 构建验证
- 构建通过，零 lint 错误

## GitPush AI 提交信息生成乱输出修复（v3 — 最终修复）

### 根因（DeepSeek API 文档审查）
DeepSeek API 规定：**必须显式传递 `thinking: {"type": "disabled"}` 才能关闭思考模式**。若省略该参数，支持思考的模型（v4 系列/reasoner）**默认开启思考**（ref: github.com/NousResearch/hermes-agent/issues/15700 + api-docs.deepseek.com）。

v2 代码传入 `enableThinking: false` 时，`buildRequestBody` 直接省略了 `thinking` 字段 → DeepSeek 仍默认开启思考 → 输出推理文本 → 正则提取失败 → 降级启发式。

### 修改（2 个文件）
- **`utils/aiApi.ts`** `buildRequestBody()`：新增显式禁用分支 —— 当 `enableThinking === false` 且是 DeepSeek 模型时，发送 `thinking: {"type": "disabled"}`（而非省略）。中间变量 `deepseekThinking` 拆分为 `isDeepSeek` + 独立判断
- **`features/gitPush/types/index.ts`**：恢复 `enableThinking: false`（现在已正确生效，会在请求体中显式禁用思考）

### 数据流
```
enableThinking: false
  → buildRequestBody 判断 isDeepSeek && enableThinking === false
  → 请求体: { thinking: { type: "disabled" } }
  → DeepSeek API 不输出 reasoning_content
  → 正则匹配 type: 描述 成功
```

### 构建验证
- 构建通过，零 lint 错误

## GitPush AI 提交信息生成乱输出修复（v2）

### 问题
v1 修复后 `enableThinking: false` 对部分模型无效，AI 仍输出推理文本，首行过滤失败后直接降级到启发式。用户看到误导性提示"未配置 AI API Key"。

### 修改（2 个文件）
- **`types/index.ts`**：移除 `enableThinking: false`（可能引发 API 报错）；后处理改为全文正则搜索 `type: 描述` 模式（`/(feat|fix|chore|docs|style|refactor|test)(:\([^)]+\))?\s*:\s*(.+)/i`），从 AI 混杂输出中智能提取 commit 信息，而非仅凭首行判断
- **`index.vue`**：修复误导性降级提示："未配置 AI API Key" → "AI 未返回有效信息，已使用启发式生成"

### 构建验证
- 构建通过，零 lint 错误

## GitPush AI 提交信息生成乱输出修复

### 问题
暂存多个文件后 AI 生成提交信息时输出冗长的分析文本（"根据 diff...修改了...涉及...这属于..."），而非简洁的 conventional commit 信息。

### 修改（1 个文件）
- **`types/index.ts`** `generateCommitMessage()` 中的 AI prompt：
  - `temperature`: 0.3 → 0.1（更确定性输出）
  - `maxTokens`: 200 → 60（提交信息只需 ~30-40 tokens）
  - prompt：新增 3 个格式示例 + "重要：只输出一行" 强调
  - systemPrompt：精简为"只输出一行...禁止输出解释、分析、额外文字"
  - **新增后处理**：只取第一行 + 过滤以"根据/分析/总结/修改/变更"开头的无效响应 → 降级到启发式

### 构建验证
- 构建通过，零 lint 错误

## GitPush 统计视图方案二：消除重复 rev-parse（loadPushStatus/loadWorkingTree 共用分支名）

### 问题
上轮优化已将 stats watch 精简为 2 个 git 命令/项目（pushStatus + workingTree），但 `loadPushStatus` 和 `loadWorkingTree` 各执行一次 `git rev-parse --abbrev-ref HEAD` 获取分支名，对同一项目重复执行相同命令。50 项目 ≈ 50 次冗余 rev-parse。

### 修改（3 个文件）
- **`types/index.ts`**：
  - 新增 `getBranch(projectPath)` 公开方法（轻量 rev-parse 封装）
  - `checkPushStatus(id, opts?)`：`opts.branch` 传入时跳过内部 rev-parse
  - `getWorkingTreeStatus(projectPath, opts?)`：`opts.branch` 传入时跳过内部 rev-parse
- **`composables/useGitPush.ts`**：
  - `loadPushStatus(id, opts?)` / `loadWorkingTree(id, skipRefresh, branch?)` 新增可选 branch 参数透传
  - 新增 `loadStatsData(id)` 复合函数：`getBranch()` 一次 → 分发给两个加载函数
  - 导出 `loadStatsData` 供 index.vue 使用
- **`index.vue`**：stats watch 改为调用 `loadStatsData(p.id)` 替代两个独立 load 调用

### 收益
- git 命令数/未缓存项目：2 → 1.5（rev-parse 从 2 次降为 1 次，status + rev-list 不变）
- 50 项目：节省 ~50 次 git rev-parse 调用

### 构建验证
- `npx vite build` 通过，零新增 lint 错误
- 仅 1 个预存 HINT（noPlatformProjects 已弃用）

## GitPush 统计视图性能优化（精简 stats watch 数据加载）

### 问题
切换统计视图（50+ 项目）卡慢，根因在 `index.vue` 的 `watch(currentView)` 对每个未缓存项目执行 5 个 git 命令（commitLog + branches + stashList + pushStatus + workingTree），其中前三个对统计视图完全无用。

### 修改（1 个文件）
- **`index.vue`** 第 1252-1268 行：移除 `loadCommitLog`/`loadBranches`/`loadStashList` 三个调用；`pending` 过滤条件从 `!commitLogs` 改为 `!pushStatuses || !workingTrees`（更精确命中缺失数据）；注释更新为"补齐统计面板所需的最小数据集"

### 收益
- git 命令数/未缓存项目：5 → 2
- 50 项目首次加载：~150+ shell 执行 → ~50-60 shell 执行
- 已缓存时：需重新执行 3 个 → **完全跳过**

### 构建验证
- `pnpm vite build` 通过，零错误零 lint

## GitPush StatsPanel 精简：移除项目管理概览和标签使用排行

移除了 StatsPanel 统计视图中的两个区块：
- **"项目管理概览"**（收藏/归档/活跃/维护/暂停 5 格网格）
- **"标签使用排行"**（标签 chip 列表）

### 修改文件（3 个）
- **`components/StatsPanel.vue`**：删除模板两个区块；删除 `StatusStats`/`TagStatItem` 接口；删除 `starredCount`/`archivedCount`/`statusStats`/`tagStats` 四个 props
- **`index.vue`**：移除 4 个 prop 绑定（`:starred-count`/`:archived-count`/`:status-stats`/`:tag-stats`）；移除 `tagStats`/`statusStats` 解构
- **`styles/StatsPanel.scss`**：删除 `.gp-mgmt-*` 和 `.gp-tag-*` 两段样式（约 72 行）

### 构建验证
- `pnpm vite build` 通过，零错误
- 零 lint 错误

## GitPush 合并待推送+未提交变更为一栏

将 StatsPanel 中"未及时推送项目"和"未提交变更项目"两栏合并为统一的"待处理项目"：

### 修改文件（4 个）
- **`components/StatsPanel.vue`**：新增 `PendingProjectItem` 接口 + `pendingProjects` computed（按 ID 合并 needsPushProjects + uncommittedProjects，totalAhead 降序 → 变更数降序）；模板合并为 6 列（项目/待推送/暂存/未暂存/未跟踪/查看）；移除未使用的 `platformLabel`
- **`styles/StatsPanel.scss`**：新增 `.gp-cell-empty`/`.gp-badge-staged`/`-unstaged`/`-untracked`；移除未使用的 `.gp-table-remote-tag`
- **`i18n`**：zh_CN + en_US 各新增 `pendingProjects`/`needsPushShort`

### 构建验证
- `pnpm vite build` 通过，零错误零 lint

## DocCountSettings 审查修复

修复了 `src/features/generalSettings/components/DocCountSettings.vue` 中 4 类 CLAUDE.md 违规：
1. **Emoji 图标** → 替换为 IconWrapper 组件（statistics/lightbulb/codeBlockColor，均已注册在 icons.ts）
2. **内联 SCSS（236行）** → 提取到 `styles/DocCountSettings.scss`，Vue 仅保留 `@use` 导入
3. **硬编码尺寸** → 替换为 `_variables.scss` Token（`$spacing-*`/`$radius-*`/`$font-size-xs`/`$font-weight-*`）
4. **font-family: monospace** → `$vp-mono`（本地声明）
5. **分隔线颜色** → `var(--b3-theme-surface-lighter)` 改为 `var(--b3-border-color)`

构建通过，零 lint 错误。

## DocCountSettings 显示格式可选功能

新增文档数显示格式可选功能，默认 `(123)`：
- **`types/storage.ts`**：新增 `DocCountFormat` 类型（`bracket|square|plain|dot`）+ `DOC_COUNT_FORMATTERS` 渲染映射
- **`modules/DocCountManager.ts`**：新增 `displayFormat` 状态、`setDisplayFormat()` 方法，`setBoxCount()` 使用 formatter 渲染，`clearAllCounts()` 正则兼容所有格式
- **`components/DocCountSettings.vue`**：新增格式选择器（4 项下拉），预览实时跟随格式变化，所有 save 方法包含 `displayFormat`
- **i18n**：`generalSettings.json` 分片文件新增 `displayFormat` 键（zh_CN: "显示格式" / en_US: "Display Format"）
- 构建通过，零 lint 错误。

### DocCountSettings 代码审查与重构

对 DocCountSettings 相关 4 个文件进行冗余合并和 DRY 重构：

**DocCountSettings.vue** (`−45 行`)：
- **消除重复 save 对象**：4 个 handler 各自构建 7 字段保存对象 → 提取 `buildSettings()` 统一构建
- **模板数据驱动**：
  - 7 个 fontSize `<option>` → `v-for="size in fontSizeOptions"`
  - 4 个 interval `<option>` → `v-for="opt in intervalOptions"`
  - 3 个 fontWeight `<option>` → `v-for="opt in weightOptions"`
- `handleIntervalChange`/`handleFontStyleChange`/`handleDisplayFormatChange` 各精简 10+ 行

**DocCountManager.ts** (`−8 行`)：
- `setBoxCount()` + `updateCountStyles()` 各 4 行重复样式赋值 → 提取 `applyStyleToSpan()` 私有方法
- `updateCountStyles` 中 `querySelectorAll` 泛型参数 `<HTMLElement>` 替代 `as HTMLElement` 断言

**DocCountSettings.scss** (`−30 行`)：
- 3 处重复卡片面板 → `@mixin card-panel`（feature-description/update-interval/font-style-settings）
- 4 处重复 section 标题 → `@mixin section-title`（setting-label/description-title/settings-title/interval-label）
- 3 处重复 select hover/focus → `@mixin select-interactive`（interval-select/style-select）
- `.description-list` 嵌套选择器合并

构建通过，零 lint 警告/错误。

## DocCountSettings 透明度设置

新增文档数透明度滑块控制：
- **`types/storage.ts`**：`DocCountSettings` 新增 `opacity: number`（0-1，默认 0.8）
- **`modules/DocCountManager.ts`**：`fontStyle` 增加 `opacity`，`setBoxCount()`/`updateCountStyles()` 应用 `style.opacity`
- **`components/DocCountSettings.vue`**：新增 `opacity` ref + range 滑块（10%-100%，步长 5%），预览实时跟随
- **`styles/DocCountSettings.scss`**：新增 `.opacity-slider`/`.opacity-value` 样式
- **i18n**：新增 `opacity` 键
- 构建通过，零 lint 错误。

### Bug 修复：重启后 displayFormat/opacity 恢复默认
`GeneralSettings.ts` 的 `applyDocCountStyle()` 启动时遗漏了 `setDisplayFormat()` 和 `opacity` 参数 → 补充了 `setDisplayFormat(settings.displayFormat)` 和 `opacity: settings.opacity ?? 0.8`。

### DocCountManager setBoxCount 批量查询优化
原 N 个笔记本 → N 次串行 SQL（`for...await fetch`），每轮网络往返 ~50ms。
优化为 1 次 `SELECT box, count(*) FROM blocks WHERE box IN (...) AND type = 'd' GROUP BY box`，然后构建 `countMap` 索引遍历 DOM。
10 笔记本场景 ~500ms → ~50ms（约 10x 提升）。

## TableStyleSettings 全面审查

审查 `TableStyleSettings.vue` 发现 5 类违规并修复：

1. **Emoji 图标**（CLAUDE.md L108）：12 处 emoji（📊✨🔲📐🎨📝🖌️⭕👁️▼🔄）→ IconWrapper 组件
   - 新增 8 个图标注册：`tableBorder/tableCell/tableHeader/tableRowOdd/tableRowEven/tableTextColor/borderRadius/starFourPoints`
   - 复用已有：`eye/eyeOff/chevronDown/refresh`
2. **动态样式**（CLAUDE.md L18）：`document.createElement("style") + appendChild` → `injectStyle(id, css) / removeStyle(id)` from `@/utils/domUtils`
3. **重复写盘**：颜色拖动时每次触发 watch → 100ms debounce + `onBeforeUnmount` 清理
4. **类型修复**：
   - `i18n?: any` → `Record<string, string>`
   - 删除冗余 `TableStyleSettingsData` 接口，改用 `storage.ts` 中的 `TableStyleSettings`
5. **SCSS**：`font-family: monospace` → `$vp-mono`，硬编码值 Token 化（`padding`/`gap`/`font-size`/`border-radius`），移除 `.toggle-arrow.expanded { }` 空块，`inset: 0` 简化，滑块删除 `box-shadow` hover 效果

构建通过，零编译错误。

### TableStyleSettings Codex UI 重构

- **模板**：自定义 toggle-switch → `SiSwitch` 组件（对齐 DocCountSettings/CodeBlockSettings 模式）
- **布局**：平铺 `.setting-row` 结构 → 卡片式布局（`.style-card` + `.preview-card`），使用 `@mixin card-panel`
- **数据驱动**：6 段重复的颜色输入行（各 10 行）→ `colorFields` 数组 + `v-for`（净减 ~45 行）
- **SCSS**：移除 `.toggle-container`/`.toggle-switch`/`.toggle-slider`/`.slider-labels`，添加 `@mixin` 共享模式，对齐 Codex 设计 Token
- **交互**：新增 `handleToggleChange` 带 `showMessage` 提示，`handleColorChange` 留空（由 watch 自动处理）
- **生成样式 Codex 化**：
  - 移除 `box-shadow` + 所有 `!important`（Codex 简洁优先）
  - 暗色主题不再硬编码 `#ffffff`，由父元素继承
  - 预览表格新增 CSS 变量驱动（`--preview-cell-border`/`--preview-header-bg`/`--preview-odd-bg`/`--preview-even-bg`/`--preview-text-color`），实时跟随设置变化


## CodeBlockSettings 全面审查重构

### 修改文件
- `src/features/generalSettings/components/CodeBlockSettings.vue` — 模板+脚本重构
- `src/features/generalSettings/styles/CodeBlockSettings.scss` — 死代码清理 + Token 化
- `src/config/icons.ts` — 新增 12 个代码块图标
- `src/index.scss` — 代码块样式 @use 迁移（同日前一次 commit）

### 变更详情

#### 1. Vue 组件（CodeBlockSettings.vue）
- **Emoji → Iconify**：12 处 emoji 图标替换为 IconWrapper + FEATURE_ICONS 注册图标（CLAUDE.md 硬规则）
- **getStyleName/getStyleDesc → computed**：原函数在 v-for 内每次渲染重建 Record 对象，改为 `computed` 预计算 `styleNameMap`/`styleDescMap`
- **watch 防抖**：`deep:true` watcher 中存储保存改为 300ms debounce（原每次按键/滑动都写盘）
- **colorFields 数据驱动**：6 个颜色行（textColor/keywordColor/stringColor/commentColor/functionColor/numberColor）54 行重复模板 → 17 行 `v-for="colorField in colorFields"`
- **presetFonts**：字体下拉选项硬编码 6 个 option → `presetFonts` 常量 + `v-for`
- **类型安全**：`styleIcons` Record 值类型从 `string` 改为 `IconKey`，消除 TS 类型错误
- **i18n Props 类型**：从 `any` 改为 `Record<string, string>`
- 移除导入中的空行、移除模板中的冗余 `as` 类型断言

#### 2. SCSS（CodeBlockSettings.scss）
- **死代码清理**：移除 ~500 行预览区/语法高亮类（`.preview-section/.preview-box/.github-header/.mac-header/.code-content/.code-line/.keyword/.function/...`），全部在模板中无对应元素
- **Token 替换**：硬编码值 → 设计 Token：
  - `padding: 12px` → `$spacing-3`
  - `font-size: 12px` → `$font-size-xs`
  - `border-radius: 8px` → `$radius-md`
  - `border-radius: 6px` → `$radius-base`
  - `border-radius: 4px` → `$radius-sm`
  - `font-weight: 600/500/700` → `$font-weight-semibold/medium/bold`
  - `gap: 8px` → `$spacing-2`，`gap: 4px` → `$spacing-1`
- **box-shadow 修复**：`.toggle-slider::before` box-shadow → border
- **合并响应式媒体查询**：原来分散在两处（line 601-660 + 921-950）→ 统一尾部
- **移除空选择器**：`.toggle-arrow.expanded { }`

#### 3. icons.ts
新增 12 个图标注册：`codeBlockEnable/ codeBlockStyle/ codeBlockDefault/ codeBlockGithub/ codeBlockMac/ codeBlockAdvanced/ codeBlockBackground/ codeBlockBorder/ codeBlockShadow/ codeBlockFont/ codeBlockColor/ codeBlockCollapse/ codeBlockHeight`

### 构建验证
- `pnpm vite build` 通过，零错误零警告
- TS 类型检查通过

## Bug 修复：字体族设置不生效

**根因**：动态 CSS (`styles.ts` `applyCodeBlockEnhancedStyles()`) 只设置了 `.code-block .hljs` 的 `font-family`，但思源编辑器的 `.hljs > code` 子元素自带 `font-family`，阻断了继承。

**修复** (`src/features/generalSettings/utils/styles.ts`)：
- 编辑区：`.protyle-wysiwyg .code-block .hljs` → 新增 `.hljs code` 和 `.hljs span` 选择器
- 预览区：`.b3-typography pre` / `pre code` → 新增 `pre span` 选择器
- `font-family` 拆分到独立规则块中（避免和其他属性的 `!important` 混在一起）

## 颜色选择卡顿修复 + 透明度支持

**卡顿根因**：`applyCodeBlockEnhancedStyles` 每次设置变更时移除+重建完整 `<style>` 元素（~140 行 CSS），拖动颜色选择器时高频触发。

**修复** (`CodeBlockSettings.vue`)：
- `applyCodeBlockStyle` 和 `applyCodeBlockCollapse` 保持立即执行（仅操作 class/style toggle）
- `applyCodeBlockEnhancedStyles` 增加 100ms 防抖（`debouncedApplyEnhanced`）

**透明度** (3 文件)：
- `storage.ts`：`CodeBlockSettings` 新增 `backgroundColorOpacity: number` (0-1)，默认 1
- `styles.ts`：新增 `hexToRgba()` 辅助函数，`opacity >= 1` 直接返回 hex，否则转 `rgba(r, g, b, o)`
- `CodeBlockSettings.vue`：背景色区域新增 10%-100% 范围滑块，带 +/− 按钮和百分比显示
## i18n 国际化文件拆分治理
- 时间: 2026-06-17 约 08:50
- 将 src/i18n/zh_CN.json (1615行/68KB) 和 en_US.json 拆分为 38~40 个 per-feature 分片文件
- 目录架构: src/i18n/{zh_CN,en_US}/featureName.json
- 创建 3 个核心脚本:
  - scripts/merge-i18n.mjs: 构建时将分片合并为思源框架所需的单一 JSON
  - scripts/verify-i18n.mjs: 校验中英文键完全对齐 + 检测重复键
  - scripts/split-i18n.mjs: 自动拆分单体 JSON 为 per-feature 分片
- vite.config.ts 中添加 buildStart 钩子，自动执行合并
- 合并产物验证: 与 git 原始版本完全一致
- 新增 npm scripts: i18n:merge, i18n:verify, i18n:split
- 后续新增 i18n 文本只需编辑对应 feature 的 JSON 分片即可

## MarkdownExportSettings 代码审查重构

### 修改文件
- `src/features/generalSettings/components/MarkdownExportSettings.vue` — 模板+脚本全面重构
- `src/features/generalSettings/styles/MarkdownExportSettings.scss` — 设计 Token 化

### CLAUDE.md 违规修复

1. **下载方式**：手动 `window.URL.createObjectURL` + `a.click()` + `setTimeout` → `triggerBlobDownload` from `@/utils/domUtils`（统一入口原则）
2. **Emoji 图标**（4 处）：
   - 📁📦 按钮标签 → Button `icon="download"` prop
   - 💡 提示图标 → `<Icon icon="mdi:information-outline">` from `@iconify/vue`
   - 📓 笔记本图标 → `<Icon icon="mdi:notebook-outline">` from `@iconify/vue`
3. **SCSS 设计 Token**：`@use "@/variables.scss" as *;`，硬编码值替换为 `$spacing-*/$radius-*/$font-size-*` Token

### 冗余消除

- **`callExportApi(apiPath, body)`**：提取公共 API 调用+响应校验（3 处重复 → 1 个函数，净减 ~45 行）
- **`startExportProgress(total)`**：提取进度条重置（2 处重复 → 1 个函数）
- **`todayString()`**：提取日期字符串生成（2 处重复 → 1 个函数）
- **`exportNotebookMd`** 改为复用 `callExportApi` + `downloadZipBlob`
- **`exportAll`** 改为复用 `callExportApi` + `downloadZipBlob`

### 死代码移除

- `defineEmits(["change"])` — 从未使用
- `Props` 接口 `i18n?: any, plugin?: any` — 从未访问
- `computed(() => Array.from(selectedNotebooks.value))` — 仅 1 处使用，改为内联

### 其他改进

- **deselectAll**：`new Set()` 替代 `.clear()`（Vue 3 `ref<Set>` 的 `.clear()` 不触发响应式更新）
- **loadNotebooks**：`(nb: any)` → `(nb: { id: string; name: string; docCount?: number })` 类型安全
- **notebook-item**：`<div>` → `<label>`（语义正确，单 `@change` 处理 toggle，消除冗余 `@click`+`@change` 双处理）
- **SCSS**：`.tip-icon` 新增 `color: var(--b3-theme-primary)`（Iconify 图标着色）

### 构建验证
零 lint 错误，TypeScript 类型检查通过。

## PromptsModal 无限内容块重构

### 修改文件（6 个）
- `src/features/floatingBox/types/index.ts` — 新增 `PromptContent` 接口（id/label/text），`Prompt` 接口 `contents: PromptContent[]` 替代 content/content2/content3，旧字段标记 @deprecated 仅用于迁移检测
- `src/features/floatingBox/types/storage.ts` — 新增 `migratePrompts()` 方法：检测旧格式 → 构建 ContentSlot[] → 清除旧字段，返回是否发生迁移
- `src/features/floatingBox/components/PromptsModal.vue` — 核心重构（698行 → ~590行）：
  - 表单：`promptForm.contents: PromptContent[]` + v-for 渲染（标签输入+文本域+上移/下移/删除按钮组）
  - 新增 `addContentBlock()`/`removeContentBlock()`/`moveContentBlock()` 方法
  - `loadPrompts` 中调用 `storage.migratePrompts()` 自动迁移旧数据
  - 所有硬编码中文→i18n 引用（60+ 处）
  - 搜索逻辑改为 `contents.some()` 遍历
- `src/features/floatingBox/styles/PromptsModal.scss` — **新建**：提取所有 vp-* 样式 + 新增动态内容块编辑器样式（`.vp-content-editor`/`.vp-content-editor-item`/`.vp-content-editor-inputs`/`.vp-content-editor-actions`/`.vp-content-editor-add`），遵循 Codex 规范（设计 Token、大写标签、0.12s 过渡、禁 box-shadow、等宽字体）
- `src/features/floatingBox/styles/index.scss` — 移除 PromptsModal 样式（745行→277行），仅保留工具栏样式+共享变量/mixins
- `src/features/floatingBox/tools/prompts.ts` — `getI18nMap()` 新增 10 个键（contents/contentLabel/addContentBlock/moveUp/moveDown/removeContent/category/loading/editPrompt），移除 content2/content3 旧键
- `src/i18n/zh_CN/skills.json` + `src/i18n/en_US/skills.json` — 新增 11 个翻译条目（内容块/内容标签/添加内容块/上移/下移/删除内容块/管理分类/添加/分类名称/分类/加载中...）

### Codex 规范遵循
- SCSS 分离：PromptsModal 样式独立文件
- 设计 Token：`$spacing-*`/`$radius-*`/`$font-size-*` 替代硬编码尺寸
- 禁 box-shadow（卡片用 border-color 过渡）
- 大写标签（10px/700/letter-spacing 0.06em）
- 等宽代码块（`$vp-mono`）
- 0.12s 过渡统一
- Focus 发光：`box-shadow: 0 0 0 2px var(--b3-theme-primary-lightest)`
- 空状态：斜体+opacity 0.35

### 构建验证
- `pnpm vite build` 通过，零错误
- `npm run i18n:verify` 通过
- 仅 1 个 HINT：迁移检测中引用已弃用 `content` 字段（预期行为）

### 后续修复
- `$font-size-2xs` 未定义 → `$font-size-xs`（`_variables.scss` 中最小变量）
- 编辑/新增弹窗宽度不足：错误使用了 `vp-modal--small`（500px），新增 `vp-modal--form`（900px），主列表模态框从 1100px→1200px

## merge-i18n 重复执行修复 + en_US 缺失键补齐

### merge-i18n 重复执行
- **根因**：`merge-i18n.mjs` 无条件写入产物文件 → Vite 文件监听检测到变化 → 触发二次 buildStart → 再次执行合并
- **修复**：写入前 `existsSync` + `readFileSync` 比较新旧内容，内容相同则跳过写入（输出 `⏭️ No changes`）
- 文件：`scripts/merge-i18n.mjs`

### en_US 缺失 60 个 i18n 键补齐
- 新建 `en_US/heatmapMarker.json`（2 键）和 `en_US/video.json`（35 键）
- 更新 7 个已有分片文件：
  - `en_US/common.json`：+blocks/assets
  - `en_US/generalSettings.json`：+enableHeatmapMarker/heatmapMarkerDescription
  - `en_US/pronunciation.json`：+pronunciation
  - `en_US/scriptLauncher.json`：+importScript
  - `en_US/superPanel.json`：+refresh/refreshing/refreshSuccess/featureEnabled/saveFailed
  - `en_US/unitConverter.json`：+unitConverterTitle/lengthInputValue/lengthFromUnit/lengthToUnit/lengthPlaceholder/lengthQuickResults
- 反向补齐 `zh_CN/qrcode.json`：+qrcodeFeature（en_US 已有）
- 验证通过：1620 叶子键完全对齐，zh_CN=40 文件、en_US=40 文件，合并后均为 667 top-level keys

## GitPush 统计视图平台状态优化

优化了 `src/features/gitPush` 统计视图的"未设置平台的项目"功能：

### 修改文件（5 个）
- **`composables/useGitPush.ts`**：新增 `platformStatusProjects` computed，返回每个项目 GitHub/Gitee/Gitea 三平台布尔状态 + missingCount（按缺失数降序）；旧 `noPlatformProjects` 标记 @deprecated 并基于新 computed 实现（仅返回 missingCount===3 的项目）
- **`components/StatsPanel.vue`**：新增 `PlatformStatusItem` 接口 + `PLATFORM_META` 常量；旧表格（名称/日期/箭头）→ 新表格（项目名 + GitHub/Gitee/Gitea 三列状态图标：`mdi:check-circle` 绿色已配置 / `mdi:close-circle-outline` 暗淡未配置）；Emoji 标签（🚀📥✅📭）替换为 Iconify 图标 + 纯文本
- **`index.vue`**：解构新增 `platformStatusProjects`，传递给 StatsPanel prop
- **`i18n` 分片**：zh_CN + en_US 各新增 20+ 键值对（platformStatus/configured/notConfigured/platforms/missingPlatforms 等统计视图完整标签）
- **`styles/StatsPanel.scss`**：`@use "@/index.scss" as *;` 导入全局 Token → 硬编码尺寸替换为 `$spacing-*`/`$radius-*`/`$font-size-*`/`$font-weight-*`（17 处）；新增 `.gp-platform-ok` / `.gp-platform-missing` / `.gp-table-cell--platform-status` 样式；状态标签新增 `display: inline-flex + gap` 图标对齐

### 构建验证
- `pnpm vite build` 通过，零错误零警告
- `npm run i18n:verify` 通过
- 零 lint 错误

## GitPush 编辑弹窗支持手动修改仓库链接

### 需求
编辑弹窗只支持修改名称、状态、标签、备注等元信息，但不允许用户手动修改 githubUrl/giteeUrl/giteaUrl 仓库链接。远程 URL 仅由 git remote 自动检测，用户无法手动修正。

### 修改（4 个文件）
- **`types/index.ts`**：`updateProjectMeta` 的 Pick 类型新增 `"githubUrl" | "giteeUrl" | "giteaUrl"`
- **`composables/useGitPush.ts`**：同上，扩展 composable 层
- **`index.vue`**：
  - 新增 `editGithubUrl`/`editGiteeUrl`/`editGiteaUrl` 三个 ref
  - `openEditDialog` 中预填项目已有 URL
  - `handleEditSave` 中传递三个 URL 字段（空字符串 → undefined 可清空）
  - 模板新增"仓库链接"区域：三行带平台图标的输入框（GitHub/Gitee/Gitea）
- **`styles/index.scss`**：新增 `.gp-edit-urls` / `.gp-edit-url-row` 样式（flex 纵向排列 + 图标左对齐）

### 构建验证
- `npx vite build` 通过，零新增 lint 错误

## GitPush 仓库配置弹窗新增编辑按钮

### 需求
仓库配置弹窗只支持查看/添加/删除远程仓库，缺少编辑已有远程 URL 的功能。用户需要先删除再重新添加才能修改 URL。

### 修改（3 个文件）
- **`types/index.ts`**：新增 `setRemoteUrl(projectPath, name, url)` 方法（`git remote set-url`）
- **`composables/useGitPush.ts`**：新增 `editRemoteOp(id, name, url)` — 调用 setRemoteUrl → refreshRemotes → loadPushStatus；导出该函数
- **`index.vue`**：
  - 新增 `editingRemoteName`/`editingRemoteUrl` 行内编辑状态
  - 远程行模板：每行新增"编辑"按钮，点击后 URL 变为输入框 + 保存/取消按钮（Enter 保存 / Escape 取消）
  - 新增 `startEditRemote(name, url)` / `handleEditRemote(id, name)` 处理函数

### 构建验证
- `npx vite build` 通过，零新增 lint 错误

## GitPush 远程检测大小写修复

### 问题
`detectRemotes` 仅通过 URL 域名识别平台（`lowerUrl.includes("gitee.com")`），如果 gitee 远程使用非标准域名、或 git remote 名称为 "Gitee"/"GITEE" 等大小写变体，则无法识别为 gitee 平台。

### 修改（1 个文件）
- **`types/index.ts`** `detectRemotes()`：平台识别逻辑从纯 URL 域名匹配改为 URL 域名 + 远程名称双重匹配（均不区分大小写）：
  - `isGithub`：`lowerUrl.includes("github.com") || lowerName === "github"`
  - `isGitee`：`lowerUrl.includes("gitee.com") || lowerUrl.includes("gitcode.com") || lowerName === "gitee"`
  - 变量重命名：`lower` → `lowerUrl`（语义更清晰）

### 构建验证
- `npx vite build` 通过，零 lint 错误

## GitPush 统计视图：合并推送状态分布与待处理项目

### 需求
统计视图中"推送状态分布"（4 格 ahead/behind/synced/noRemote）和"待处理项目"（表格）是两个独立 section，占用大量垂直空间。合并为紧凑布局。

### 修改（4 个文件）
- **`components/StatsPanel.vue`**：
  - 删除独立"推送状态分布"section（4 格网格）
  - "待处理项目"section 顶部新增 `.gp-status-bar` 状态条（4 个 chip：ahead/behind/synced/noRemote，含图标+数值）
  - pendingProjects.length === 0 时显示 `.gp-status-all-clear`（"所有项目状态正常"）
- **`styles/StatsPanel.scss`**：
  - 删除 `.gp-status-grid`/`.gp-status-cell`/`.gp-status-cell-num`/`.gp-status-cell-label` 及相关颜色变体（~35 行）
  - 新增 `.gp-status-bar`（flex 横向排列）、`.gp-status-chip`（compact 8px 高 chip，含 `--ahead/--behind/--synced/--none` 颜色变体）、`.gp-status-all-clear`（斜体空状态）
- **i18n**：`zh_CN` + `en_US` 各新增 `allClear` 键

### 构建验证
- `npx vite build` 通过，零新增 lint 错误
- `npm run i18n:verify` 通过

## GitPush 全面代码审查重构（冗余合并 + 性能优化）

### 审查范围
基于 code-explorer 子代理全面审计，对 gitPush 功能（21 文件 / ~7043 行）进行 CLAUDE.md 合规审查，识别 6 处冗余逻辑并逐个重构。

### 重构清单（5 项，净减 ~95 行）

**1. 提取 useDirectoryPicker composable**（消除 ~60 行重复）
- **`composables/useDirectoryPicker.ts`** [NEW]：`pickDirectory(title)` 封装 Electron 原生 + webkitdirectory 降级
- **`index.vue`**：`selectDirectory()` + `selectScanDirectory()` 各从 ~50 行 → 3 行调用

**2. 统一远程平台元数据**（消除 3 处 PLATFORM_META/REMOTES 重复定义）
- **`types/index.ts`**：新增 `PLATFORM_META` 导出常量（key/icon/label/remoteProp/urlProp），单一数据源
- **`index.vue`**：REMOTES → `PLATFORM_META.map(...)` 派生；删除 RemoteKey 类型
- **`StatsPanel.vue`**：删除本地 PLATFORM_META，改用 types 导入；删除未用 formatDate
- **`useGitPush.ts`**：`platformLabel()` 改用 PLATFORM_META 查找
- **`types/index.ts`** `checkPushStatus()`：3 个 if 分支 → `for...of PLATFORM_META`

**3. 合并 formatPushOutput / formatPullOutput**（消除 ~20 行重复）
- **`useGitPush.ts`**：统一 `formatGitOutput(target, id, opName, entries)` + 辅助函数
- push/pull 4 个函数各精简 3-6 行

**4. 死代码清理**
- `StatsPanel.vue`：移除未使用的 `formatDate()` 函数
- `index.vue`：移除未使用的 `RemoteKey` 类型别名

**5. README.md 全面更新**
- 补齐缺失组件/文件、新增性能优化章节、更新 API 表

### 构建验证
- `npx vite build` 通过，零新增 lint 错误（仅 1 个预存 deprecated hint）
- `npm run i18n:verify` 通过

## GitPush 功能扩展计划

生成了 5 项功能扩展计划，保存至 `.codebuddy/memory/gitpush-extension-plan.md`：

| 优先级 | 功能 | 工作量 | 改动文件 |
|--------|------|--------|----------|
| P1 高 | 快捷操作工具栏（hover 显示拉取/推送/Stash 图标） | 2h | 1 文件 |
| P2 中 | Git Tag 管理（创建/列表/推送/删除） | 3h | 5 文件 + 1 新组件 |
| P2 中 | 冲突检测与解决引导（merge conflict 解析 + 中止/保留/打开） | 2h | 3 文件 |
| P3 低 | 项目组/工作区（批量分支切换/拉取/推送） | 4h | 5 文件 + 2 新组件 |
| P3 低 | 提交信息模板（预置 + 自定义，占位符支持） | 1.5h | 2 文件 |

## GitPush 三期功能扩展实现：Tag 管理 + 冲突检测 + 提交信息模板

## 新增文件（1 个）
- **`components/TagPanel.vue`** [NEW]：Tag 管理面板（创建表单 + Tag 列表 + 推送/删除操作）

### 修改文件（8 个）

**类型与存储层**：
- **`types/storage.ts`**：新增 `TagInfo`/`ConflictFile`/`CommitTemplate` 接口 + `DEFAULT_TEMPLATES`（5 个预置模板）+ `commitTemplates` TypedStorage
- **`types/index.ts`**：导出新类型；`GitPushManager` 新增 Tag（getTags/createTag/deleteTag/pushTag）、冲突（hasConflict/getConflictFiles/abortMerge/resolveConflictFile）、模板（getCommitTemplates/saveCommitTemplates）9 个方法

**响应式层**：
- **`composables/useGitPush.ts`**：新增 `tagsCache`/`tagLoading`/`conflicts`/`commitTemplates` 4 个 ref + 12 个方法；pullToAll/pullSingle 后自动异步检测冲突

**UI 层**：
- **`index.vue`**：卡片新增 TagPanel + 冲突警告条（红色背景 + 文件列表 + 保留本地/远程/中止按钮）；WorkingTreePanel 传入 `:commit-templates`；onMounted 加载模板；6 个 handler
- **`components/WorkingTreePanel.vue`**：新增 `commitTemplates` prop + 模板下拉选择器（支持 `{branch}`/`{files}` 占位符）

**样式**：
- **`styles/index.scss`**：新增 `.gp-tag-*`/`.gp-conflict-*` 约 100 行
- **`styles/WorkingTreePanel.scss`**：新增 `.wt-template-row`/`.wt-template-select`

**i18n**：zh_CN + en_US 各新增 7 键（createTag/noTags/conflictDetected/keepOurs/keepTheirs/abortMerge/selectTemplate）

### 构建验证
- `npx vite build` 通过，零新增 lint 错误
- `npm run i18n:verify` 通过
