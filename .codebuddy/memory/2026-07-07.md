# 2026-07-07

## textDiff SCSS 审查与重构
- 将 `src/features/textDiff/index.vue` 的内联 `<style scoped>`（原 469~835 行）整体提取到 `src/features/textDiff/styles/TextDiff.scss`，vue 改为 `@use './styles/TextDiff.scss';`（保留 scoped，视觉零变化）。
- 合并冗余：新增 `@mixin codex-btn-base`（toggle-btn/action-btn/file-btn 共用静态基础样式）、`@mixin flex-row($gap)`（header-left/right 共用）；`$codex-mono` → 全局 `$vp-mono`。
- Token 替换：硬编码圆角 `6px→$vp-radius`、`4px→$radius-sm`；可精确映射的间距 `16px→$spacing-4`、`8px→$spacing-2`、`4px→$spacing-1`；`12px→$font-size-xs`、`font-weight 500→$font-weight-medium`、`line-height 1.5→$line-height-normal`。
- 性能/精简：`.diff-viewer` 中 `:deep(.vue-diff *)` 已覆盖全部后代，删除其后冗余的 4 个 `.d2h-*` 具体选择器。
- 故意保留（无精确 Token 且为保持视觉不变）：`10px/14px` padding、`6px` padding、`5px` 圆角、`29px` 定位、`24px` 图标按钮尺寸、`11px/13px` 字体。
- 验证：`eslint src/features/textDiff` = 0 errors（仅脚本层既有 warning）；`tsc --noEmit` 无 textDiff 新增错误。codex-ui-style-guide 合规校验通过。
- 注意：`$vp-radius`/`$vp-mono` 已在 `@/variables.scss` 全局定义，按 CLAUDE_RULES 直接继承，无需在模块内重复声明（与 codex-ui-style-guide skill 的 Rule 5 说法冲突，以 CLAUDE_RULES + 实际变量定义为准）。

## gitPush 模块代码审查与优化（6个文件）
修改范围：GitPushManager.ts / utils.ts / useGitOps.ts / useCommitLog.ts / useProjectFilters.ts / useGitTagsConflicts.ts / index.vue，共7个文件。

### 逻辑漏洞修复（5项）
- **destroy() 内存泄漏**：新增 `this.networkWaitQueue.length = 0` 清理网络等待队列闭包
- **checkCanPushToCloud 跨设备 bug**：`project.path` → `resolveValidPath(project)` 与其它方法统一
- **tryRemoteOp signal 监听器泄漏**：重试延时定时器 resolve 后调用 `removeEventListener("abort")`
- **scanForGitRepos(BFS) 符号链接无限循环**：新增 `!entry.isSymbolicLink()` 条件跳过
- **remoteOpAll catch 缺失 return**：添加 `return { success: false }` 防止 undefined
- **remoteOpSingle 缺失 try-catch**：新增 catch 块设置进度为 "fail" 并记录输出
- **handleExpand 失败不可重试**：`expandedProjects.add()` 移到成功回调后，失败不标记

### 冗余消除（4项）
- **useGitTagsConflicts 重复导入**：`findProject/normalizePathForDedup` + `resolveValidPath` → 合并为一行
- **toggleTagFilter 重复**：删除 index.vue 本地函数，改用 useProjectFilters 导出的 `toggleTag: toggleTagFilter`
- **平台远程名称提取 4 处重复**：新增 `getProjectRemoteNames(project)` 工具函数，替换 GitPushManager 3 处 + index.vue 1 处内联循环
- **console.log 调试日志**：移除 handleGitOp 中 2 行 console.log，保留 catch 中 console.error

### 死代码清理（2项）
- **lastRefreshTime Map**：声明后从未用于防抖检查，已删除声明；REFRESH_COOLDOWN_MS 仅用于全局防抖故保留
- **headHashes 未清理**：handleRemove 中 `lastRefreshTime.delete()` → `delete headHashes.value[project.id]`

### 性能优化（3项）
- **importScanResults O(N*M)→O(N)**：`selectedPaths.includes()` → `new Set(selectedPaths).has()`
- **filteredGroups 双重 map**：`.map(filter).map(sort)` → `.map(sort(filter))` 单次遍历
- **remoteOpAll PLATFORM_META 遍历**：改用 `getProjectRemoteNames` 只遍历已配置远程

### 验证
- ESLint: 0 errors（全部修改文件）
- 4 个批次任务全部完成

## gitPush 拆分刷新按钮（双入口设计）
- 需求：原"一键刷新全部 6 项"太慢，拆分为 4 个独立刷新：工作空间/提交日志/标签/远程状态
- 双入口：
  1. 原顶部刷新按钮改造为下拉菜单（`.gp-refresh-wrap` + `.gp-refresh-popover`，复用 IDE 菜单模式），5 个菜单项（4 细分 + 全部刷新）
  2. 各 Section 内部独立刷新按钮：远程状态区（REMOTES label 旁）、WorkingTreePanel 摘要条（WORKING TREE 右侧）、提交历史区（COMMIT LOG 标签旁）、TagPanel 头部（TAG 标签旁）
- 4 个独立操作：
  - 工作空间 → `loadWorkingTree(id, false, branch)`，新增 `workingTreeLoading`
  - 提交日志 → `loadCommitLog(id)`，复用 `commitLogLoading`
  - 标签 → `loadTags(id)`，复用 `tagLoading`
  - 远程状态 → `refreshRemotes(id)` + `loadPushStatus(id, {fetchFirst:true,branch})`，新增 `remoteStatusLoading`
- 修改文件（8 个）：
  - `index.vue`：新增 2 个 loading ref、openRefreshMenu Set、toggleRefreshMenu、4 个 handler、扩展 closeIdeMenuOnOutside
  - `ProjectCard.vue`：下拉菜单模板 + 远程区按钮 + 3 新 props + 5 新 emits + Section 事件透传
  - `WorkingTreePanel.vue`：摘要条/提交历史区刷新按钮 + workingTreeLoading prop + 2 新 emits
  - `TagPanel.vue`：头部刷新按钮 + refresh emit
  - `styles/index.scss`：`.gp-refresh-wrap/popover/item/divider`、`.gp-remotes-label`、`.gp-section-refresh`
  - `styles/WorkingTreePanel.scss`：`.wt-section-refresh`、`.wt-commit-history-header/label`
  - `zh_CN/en_US gitPush.json`：5 个新翻译键
- 验证全通过：i18n:verify ✅ (1830 keys aligned)、validate:icons ✅ (118 icons)、ESLint 0 errors（修改文件）、tsc --noEmit ✅、事件透传链路 6 项检查全部完整

## gitPush PanelHeader 刷新按钮拆分（本地/远程/全部）
- 需求：Header 单一刷新按钮改为下拉菜单，分离"刷新本地状态（快）"和"刷新远程状态（慢，含 git fetch）"
- 修改文件（4 个）：
  - `PanelHeader.vue`：刷新按钮→`.gp-header-refresh-wrap` 下拉（3 选项：本地/远程/全部），新增 `showRefreshMenu`/`refreshingAllLocal`/`refreshingAllRemote` props + `refreshAllLocal`/`refreshAllRemote` emits + `update:showRefreshMenu` v-model
  - `index.vue`：新增 3 个 ref + `handleRefreshAllLocal`（调用 silentRefreshAll，快）+ `handleRefreshAllRemote`（遍历项目 fetchAllRemotes，慢）+ 扩展 closeIdeMenuOnOutside
  - `PanelHeader.scss`：刷新下拉菜单样式嵌套在`.gp-header-refresh-wrap`内，避免与 ProjectCard 的`.gp-refresh-popover`定位冲突（header 用 right:0，card 用居中）
  - `zh_CN/en_US gitPush.json`：新增 `headerRefreshLocal`/`headerRefreshRemote` 翻译键
- 验证全通过：tsc --noEmit ✅、ESLint 0 errors、i18n:verify ✅ (1837 keys)

## wordQuery 模块代码审查与优化（12个文件）
修改范围：types/index.ts / utils/codeTranslation.ts / index.vue / 4个子组件 / 2个SCSS文件。

### 冗余消除（3项）
- **重复 Props 接口**：4个子组件各自的 `Props { i18n; plugin }` → 提取为 `WordQueryComponentProps` 到 `types/index.ts`，子组件统一 `defineProps<WordQueryComponentProps>()`
- **中间变量**：移除 `namingStyles = NAMING_STYLES` / `commentStyles = COMMENT_STYLES`，模板直接用顶层 import 绑定
- **@input="clearError" 双重触发**：4个子组件模板移除 `@input="clearError"`，`clearErrorOnInput()` watch 已覆盖输入变化

### 工具函数迁移
- `generateAbbreviation` 从 `CodeTranslationPanel.vue` → `utils/codeTranslation.ts`（纯函数，符合分层规则）

### 逻辑漏洞修复
- `speechSynthesis` 未清理：`onUnmounted` 新增 `speechSynthesis.cancel()`，组件销毁时停止播放

### SCSS Token 化（2个文件，~950行）
- `styles/index.scss`（437行）：`14px→$font-size-sm` / `16px→$font-size-base` / `12px→$font-size-xs` / `600→$font-weight-semibold` / `500→$font-weight-medium` / `padding/gap/margin 16/12/8/4→$spacing-4/3/2/1` / `border-radius 6px→$radius-base`
- `styles/codeUtils.scss`（516行）：等宽字体 `"JetBrains Mono"..."→$vp-mono` + 同上 Token 替换；保留 `13px`/`6px`/`10px` 等无精确 Token 的值

### 验证
- tsc-plugin: 0 new errors（剩余 6 个预存在图标类型错误，非本次引入）
- clearError unused warnings 全部清除
- 功能逻辑零变化

## wordQuery 迁移至 toolCollection
- 将 wordQuery 功能从独立 Dock 面板完整迁移到 toolCollection 底部面板作为第 3 个 Tab 工具
- 文件移动：`src/features/wordQuery/`（14 个文件）→ `src/features/toolCollection/tools/wordQuery/`，旧目录已删除
- 自包含改造：
  - `utils/api.ts`：新增 `buildQueryPrompt`/`buildTranslatePrompt`/`isEnglishWord`（从 WordQueryManager 提取）
  - `types/index.ts`：移除 `WordQueryManager` 类（Dock 注册相关），保留 `LANGUAGE_MAP` 和 `WordQueryComponentProps`
  - 删除 `wordQuery/index.ts`（Dock 注册入口）
  - `index.vue`：移除 `onQuery`/`onTranslate` props，改为直接 `callWordQueryAPI() + getApiConfigFromPlugin(plugin)`
- 注册链更新：
  - `toolCollection/index.vue`：新增 WordQueryTool 导入/模板/工具注册表条目；`i18n` 类型改为 `Record<string, any>` 支持嵌套对象
  - `src/index.ts`：移除 `registerWordQuery` 导入、调用和 `__wordQuery.destroy()` 清理
  - `src/features/index.ts`：移除 `registerWordQuery` 导出；`wordQuery` 从 `_Registered` 移到 `_ConfigOnly`
- 保留不变：`enableWordQuery` 开关、`wordQuery` 图标、全部 i18n 键（112 个）
- 验证：无 lint 错误，tsc 编译零新增错误

## 从 superPanel 移除 wordQuery 开关
- wordQuery 迁移至 toolCollection 后，其 FEATURE_CONFIG 条目仍导致 superPanel 中显示"单词查询"卡片/开关
- 修改：从 `src/features/config.ts` 移除 wordQuery 条目（与 base64Image 处理方式一致）；从 `src/features/index.ts` 的 `_ConfigOnly` 移除 wordQuery
- 保留：`enableWordQuery` 设置键、`FEATURE_ICONS.wordQuery` 图标（toolCollection 模板仍引用）、全部 i18n 键
- 验证：lint 0 errors

## 规则更新：AI 不执行 pnpm vite build 和 pnpm lint
- 在三个规则文件（CODEBUDDY.md、CLAUDE_RULES.md、CLAUDE.md）的验证链条/构建与验证章节添加重要声明：AI 不得执行 `pnpm vite build` 和 `pnpm lint`，这些验证由用户自行完成
- 保留 `pnpm i18n:verify`、`pnpm validate:icons`、`npx tsc --noEmit` 等快速验证命令可正常使用

## wordQuery SCSS 审查与重构（2026-07-10）
- 修改 2 个 SCSS 文件（`codeUtils.scss` 519 行 → 516 行，`index.scss` 501 行 → 494 行），lint 0 errors。
- **codeUtils.scss 重构**：
  - 新增 3 个局部变量（`$code-font-size: 13px`、`$code-line-height: 1.6`、`$compact-gap: 6px`）集中管理无精确 Token 的硬编码值
  - 新增 `@mixin label-row`（5 处引用：`.input-label`、`.style-label`、`.examples-label` 直接 @include；`.result-label` 和 `.section-title` 因 `font-weight: $font-weight-semibold` 不同保留显式但改用 `$compact-gap`）
  - 新增 `@mixin suggestions-list-base` 消除 2 处 `.suggestions-list` 完全重复声明
  - 10 处 `gap: 6px` → `$compact-gap`；8 处 `font-size: 13px` → `$code-font-size`；3 处 `line-height: 1.6` → `$code-line-height`
- **index.scss 重构**：
  - 2 处 `line-height: 1.5` → `$line-height-normal`（值相同，零视觉变化）
  - 移除 `br { display: block; margin-bottom: $spacing-2; content: ""; }` 冗余 hack 样式（4 行）
  - 480px 响应式中 `gap: 6px` → `$spacing-1`（4px，窄屏按钮间距微调 2px）

## wordQuery 冗余 CSS 清理
- 审查 2 个 SCSS 文件交叉比对 5 个 Vue 模板，发现 codeUtils.scss 中 2 处未引用 CSS：
  1. `.panel-header .panel-title`（10 行）— 4 个子组件均未使用
  2. `.input-section .input-label`（3 行）— 输入区已无配套 label
- 已删除，lint 0 errors
- 额外发现模板中 8 个无 CSS 规则的残留 class（api-key-toggle、query-input、panel-header-title、loading-spinner-large、dropdown/dropdown-menu/dropdown-item、translation-textarea），已全部清理。同步将 handleClickOutside 的 `.dropdown` 选择器改为 ref.contains() 方式。lint 0 errors

## CodeTranslationPanel 翻译结果与备选方案同行
- 需求：翻译结果（main-result）和备选方案（suggestions）改为同一行并排显示
- 修改 `codeUtils.scss`：
  - `.code-translation-panel .result-content` 覆盖为 `flex-direction: row; align-items: flex-start`
  - `.main-result` / `.suggestions` 各设 `flex: 1; min-width: 0` 等分宽度
  - `.suggestions` 分隔从 `border-top + padding-top` 改为 `border-left + padding-left`
  - 响应式 ≤480px 恢复列布局 + 顶部边框
- lint 0 errors

## CodeTranslationPanel 紧凑结果布局重构
- 需求：翻译结果仍然太占地方，要求彻底重构结果区域为紧凑布局
- **模板重构**（CodeTranslationPanel.vue）：
  - 移除 4 个 `.result-item` 独立卡片块（原文/译文/缩写/风格，各有 padding+背景+border-radius）
  - 改为 3 层紧凑结构：
    1. `.result-meta`：原文 + | + 风格（单行元信息，xs 字号）
    2. `.result-primary`：译文（primary-light 背景）+ 缩写标签（primary 实心底色白色文字，可点击复制）
    3. `.suggestions`：标签 + 横向 flex-wrap 标签云（`.suggestion-tag`，border 样式，hover 变蓝）
- **SCSS 覆写**（codeUtils.scss 第 182-293 行）：
  - `.code-translation-panel .result-content` 改回 `flex-direction: column`
  - 旧 `.main-result`/`.result-item`/`.suggestions` 卡片样式全部替换为紧凑覆写
  - `.suggestion-tag` 无背景、窄边框、`border-radius: $radius-sm`、hover 高亮
- lint 0 errors（vue + scss 两文件）

## CodeTranslationPanel 表格式行布局 UI 重构
- 需求：去掉外层卡片壳，改用表格式/行格式显示翻译结果
- **模板重构**（CodeTranslationPanel.vue 第 68-143 行）：
  - 移除 `.result-content` / `.result-meta` / `.result-primary` / `.suggestions` 嵌套结构
  - 改为 `.result-rows` 容器 + 4 个 `.result-row`（每行 label + value）：
    1. 原文 — 原文中文
    2. 风格 — 命名风格名
    3. 译文（highlight）— 译文 + 缩写实心标签
    4. 备选 — 横向标签云
- **SCSS 覆写**（codeUtils.scss 第 182-298 行）：
  - `.code-translation-panel .result-section`：padding:0 / background:none / border-radius:0 去掉卡片壳
  - 新增 `.result-rows`：border+radius 容器，overflow:hidden 裁剪内部圆角
  - 新增 `.result-row`：flex 行 + border-bottom 分隔线，last-child 无底线，highlight 用 primary-light 背景
  - 新增 `.row-label`：min-width:52px 固定左列宽度，xs 字号灰色
  - 新增 `.row-value`：flex:1 右列伸缩，mono 字体，highlight 下蓝色加粗
  - 新增 `.row-abbr`：译文行右侧缩写实心标签（primary 背景白色文字）
  - 新增 `.row-tags`：备选行内 flex-wrap 标签容器
  - `.suggestion-tag` 保留精简 border 标签样式
- lint 0 errors（vue + scss 两文件）
