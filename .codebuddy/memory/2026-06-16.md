# 2026-06-16

## superPanel 功能 CLAUDE.md 规范审查与修复

按照 CLAUDE.md 规范对 `src/features/superPanel` 进行全面审查并修复所有违规项：

### 修复内容
1. **跨功能导入违规**：移除 `types/index.ts` 中 `import { getTextDiffManager } from "@/features/textDiff"`，改为 ACTION_EVENT_MAP 事件调度 + App.vue 监听模式
2. **全局样式入口**：`index.vue` 的 `@use './styles/index.scss'` 改为 `@use "@/index.scss" as *;`
3. **box-shadow → border**：`styles/index.scss` 中 4 处 focus 样式从 box-shadow 替换为 outline 实现
4. **版本管理拆分**：从 SuperPanelManager 提取 `composables/useVersionManager.ts`（~90 行），职责单一化
5. **移除死代码**：移除 `__superPanel` 全局变量挂载（无外部引用）
6. **Token 统一**：`feature-card.scss` 中 7 处硬编码尺寸替换为 `$spacing-*` / `$vp-radius` 设计 Token
7. **i18n 去硬编码**：移除 `index.vue` 中状态标签的中文硬编码回退

### 涉及文件
- `src/features/superPanel/types/index.ts` — 移除跨功能导入、版本管理委托
- `src/features/superPanel/index.ts` — 移除 __superPanel
- `src/features/superPanel/index.vue` — 全局样式 + i18n
- `src/features/superPanel/styles/index.scss` — 4处 box-shadow → outline
- `src/features/superPanel/styles/feature-card.scss` — 7处硬编码 → Token
- `src/features/superPanel/composables/useVersionManager.ts` — **新文件**
- `src/features/index.ts` — 导出 getTextDiffManager
- `src/App.vue` — 新增 openTextDiff 事件监听

构建通过（`pnpm vite build`），零 lint 错误。
