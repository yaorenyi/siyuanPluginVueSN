# dataBackup → s3Backup 合并完成

## 日期：2026-07-06

### 变更概述
将 `src/features/dataBackup`（本地 ZIP 压缩备份 + 自动备份定时器）完全迁移合并到 `src/features/s3Backup` 模块中，dataBackup 模块已彻底删除。

### 具体变更

#### s3Backup 模块增强（7 个文件修改）
- **modules/BackupManager.ts** — 合并版备份管理器：保留文件扫描 + 新增 ZIP 打包（performFullBackup）/ 本地备份管理（scanBackupDir/deleteBackupFile），统一 scanDirectory 实现
- **types/index.ts** — 新增 BackupMode / AutoBackupSettings / LocalBackupInfo 接口，S3BackupStorage 新增 autoBackupSettings 和 backupHistory 存储槽
- **index.ts** — S3Backup 类增强：新增自动备份定时器（分钟/小时/天）、autoBackupTrigger 事件处理、持久化 Modal 模式
- **index.vue** — 主面板重构：新增备份模式选择区（多选复选框）、自动备份设置区、本地备份历史列表，手动备份根据 backupMode 分发
- **composables/useS3Backup.ts** — phaseLabel 扩展 packing/compressing/saving
- **styles/index.scss** — 新增备份模式选择器、自动备份设置区样式
- **i18n** — zh_CN + en_US 各从 54 → 78 keys（新增 24 个本地备份专属 key）

#### 删除的 dataBackup 模块（9 个文件）
- src/features/dataBackup/ 整个目录（7 个文件）+ 2 个 i18n 分片文件

#### 注册链清理（7 个文件）
- **config.ts** — 删除 dataBackup FEATURE_CONFIG 条目，更新 s3Backup 描述
- **icons.ts** — 删除 dataBackup 图标条目
- **features/index.ts** — 删除 registerDataBackup 导出 + _Registered 移除 "dataBackup"
- **index.ts** — 删除 import / registerDataBackup(this) / __dataBackup 清理代码
- **statusBar/index.vue** — 删除 dataBackup 条目（s3Backup 条目保留）
- **superPanel.json** (zh_CN + en_US) — 删除 dataBackup 键

### 合并后的功能
- 备份模式：可多选「本地 ZIP」+「S3 上传」，灵活组合
- 手动备份：一键触发，顺序执行所选模式
- 自动备份：支持每分钟/每小时/每天，可设置时间 + 保留份数
- 本地备份历史：展示 data-backup/ 目录下 ZIP 文件，支持删除
- S3 云端备份列表：下载/恢复/删除（功能不变）

### 验证结果
✅ pnpm lint — 0 errors  
✅ pnpm i18n:verify — 1820 keys 中英文对齐  
✅ pnpm validate:icons — 118 icons 全部有效  
✅ npx tsc --noEmit — 编译无错误

### 修复 S3 备份源路径（同日）
- **问题**：S3 备份扫描的是 workspaceRoot/data/ 目录逐文件上传，而非 data-backup/ 下的 ZIP 文件
- **修复**：BackupManager.getWorkspaceFiles() 扫描源从 this.dataPath 改为 this.backupDir
- **影响**：S3 上传现在上传的是本地备份生成的 ZIP 包，而非 data/ 原始文件
- **lint**：无新增错误

### S3 备份面板增加路径预览标识（同日）
- **目的**：让用户直观理解「本地备份目录」和「S3 上传子路径」两个配置项的实际含义
- **改动**（4 个文件）：
  - `index.vue` — 新增 resolvedLocalBackupPath / resolvedS3Path 两个 computed 属性动态生成完整路径；模板新增 path-preview 区域（灰色背景 code 块显示实际路径）+ form-label-desc 说明行
  - `styles/index.scss` — 新增 .path-preview / .path-preview-label / .path-preview-value / .form-label-desc 样式
  - `zh_CN/s3Backup.json` + `en_US/s3Backup.json` — 新增 localBackupDirDesc / s3SubPathDesc / pathPreview 3 个键，优化 localBackupDirHint / s3SubPathHint 提示文本更通俗
- **验证**：pnpm lint 0 error、i18n:verify 1828 keys 对齐

### s3Backup 代码审查与修复（同日）

对 s3Backup 模块进行全面审查，发现 14 个逻辑漏洞 + 14 处冗余/死代码，修复 21 项。

#### 逻辑漏洞修复（10 项）

| # | 问题 | 修复 |
|---|------|------|
| A1 | keepBackupCount 只截断内存不删磁盘旧 ZIP | 超出时调用 backupManager.deleteBackupFile() |
| A2 | useDateFolder 对本地 ZIP 无效（死配置） | 集成到 BackupManager.performFullBackup，支持日期子文件夹 |
| A3 | 本地备份列表 time 用上次时间 | 改用 new Date().toLocaleString() |
| A4 | S3 重复上传 data-backup/ 全部历史 ZIP | 本地+S3 同时勾选时只传本次生成的 ZIP |
| A5 | daily 定时器精确分钟匹配可能错过整点 | 改为宽松窗口（currentTotal >= targetTotal） |
| A6 | 防重复状态是闭包变量，重启定时器丢失 | 提升为实例字段 lastExecutedHour/lastExecutedDateStr |
| A7 | 自动备份失败静默无提示 | 添加 console.warn 日志 |
| A8 | S3 凭证 accessKey/secretKey 明文存储 | 复用 settingsCrypto 的 encryptSetting/decryptSetting 加密 |
| A9 | handleDownload/handleRestore 硬编码 data-backup | 改用 localBackupDir.value |
| A11 | deleteLocalBackup 按 name 过滤可能误删 | 改为按 path 过滤 |

#### 冗余/死代码清理（11 项）

| # | 问题 | 操作 |
|---|------|------|
| B1 | workspacePath/workspaceRoot 双字段冗余 | BackupManager 移除 workspacePath 字段，保留单字段 |
| B2 | AutoBackupSettings 接口+默认值+存储槽全死代码 | 删除接口/常量/存储槽（430→400 行） |
| B6 | performS3OnlyBackup 与 performS3Backup 重复 | 合并为 triggerS3OnlyUpload，去重 isConfigured 检查 |
| B7 | handleDownload/handleRestore 核心逻辑重复 | 提取 downloadToLocalDir() 公共方法 |
| B8 | BackupManager.updateWorkspacePath 单参死方法 | 删除 |
| B10 | getNodeModules() 重复调用 4 次 | 复用顶层缓存 node 变量 |
| B11 | 时间戳格式不一致（2位年 vs 4位年） | 统一为 4 位年份，formatTimestamp 提取到工具函数 |
| B14 | payloadHash 两个分支相同 | 合并为单行 return sha256Hex(body) |

#### 涉及文件
- index.vue（主修复）+ index.ts + BackupManager.ts + types/index.ts + types/s3Client.ts
- 所有修改通过 lint 0 error 验证

### 冗余/死代码进一步清理（同日第二轮）

用户要求"删除冗余，死代码"，补充清理以下 4 项：

| # | 问题 | 操作 |
|---|------|------|
| B4 | `saveSettings()` 是 `saveWorkspaceSettings()` 的空壳（5 处调用） | 模板和内联调用全改为 `saveWorkspaceSettings`，删除包装函数 |
| B5 | `handleTestConnection(config)` 单行转发 `testConnection(config)` | 模板 prop 直接传 `testConnection`，删除转发函数 |
| B9 | composable 中 `isConnected`/`isConnecting` ref 从未导出，完全死代码 | 删除 2 个 ref + 4 处写入，`testConnection` 简化 |

涉及文件：`index.vue`（-10 行）、`composables/useS3Backup.ts`（-8 行）
lint 0 error

### s3Backup 公共组件替换与样式清理（同日第三轮）

将 s3Backup 模块中所有原生 HTML 表单元素替换为 `src/components/` 公共组件：

#### 组件替换统计
- **Button**：15 个 → 使用 variant(primary/ghost/danger) + size(small) + loading/disabled
- **Input**：12 个（含 text/password/number/time）→ 使用 size="small"，密码字段用 showPassword 内置切换
- **Switch**：7 个 checkbox → :modelValue+@update:modelValue（reactive 属性）/ v-model（ref 属性）
- **Select**：2 个 → 使用 :options computed 数组

#### 样式清理
- **index.scss**：删除 ~200 行（.vp-btn 系列、.vp-spin、.action-btn、.refresh-btn、.form-input--sm、.form-input.narrow、.form-select.narrow、.form-checkbox*、.select-path-btn、.open-folder-btn、.s3-backup-close-btn、.form-label-desc）
- **S3ConfigForm.vue 内联样式**：删除 ~80 行（.form-input、.password-input-wrapper、.toggle-password-btn、.form-checkbox*、.guide-toggle-btn、.guide-close-btn、.form-label）
- **保留样式**：.form-hint、.path-preview*、.backup-list*、.progress-*、.info-*、.card-section、.section-header、.backup-actions-row、.settings-row、.inline-label 等容器/文本样式

#### 关键细节
- IconKey 使用 `"close"` / `"help"` 而非原始 Iconify 名称
- S3ConfigForm 移除 `showSecret` ref（Input 内置 password 切换）
- eslint + tsc 无新增错误

### s3Backup 自动备份逻辑审查与修复（同日第四轮）

对自动备份设置进行全面逻辑审查，发现并修复 6 个问题：

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| B12 | hourly 模式 `currentMinute===0` 窗口过窄，tick 偏移就错过 | 移除 strict 分钟匹配，改用 `lastExecutedHour` 防重复 | index.ts |
| B13 | `performManualBackup` finally 重复调用 `saveWorkspaceSettings` | 删除 finally 中的重复调用，try 块已保存 | index.vue |
| B14 | watch 初始化时触发不必要的定时器重启（与 initAutoBackup 冲突） | 新增 `isInitialLoad` ref，加载完成后才允许 watch 触发 | index.vue |
| B15 | `keepBackupCount` 仅清理本地 ZIP，S3 云端无清理 | 在 `performS3Backup` 末尾按 lastModified 排序裁剪旧备份 | index.vue |
| — | `lastBackupTimestamp` 未在组件加载时恢复（重启后时间基准丢失） | `loadWorkspaceSettings` 返回类型 + 组件恢复各补充该字段 | index.ts + index.vue |
| — | `loadWorkspaceSettings` 返回类型不含 `lastBackupTimestamp`（读写不对称） | 返回类型声明 + 值 + 兜底分支全部补充 | index.ts |

lint 0 error，2 个文件（index.ts + index.vue）修改

### 撤销 S3 云端备份清理（同日第五轮）
- 根据用户要求撤销 B15 修复：移除 `performS3Backup()` 末尾的 S3 云端旧备份自动清理代码（798-813 行）
- lint 0 error

### s3Backup SCSS 审查与合并（同日第六轮）
- **问题**：S3ConfigForm.vue 第 289-449 行有约 160 行内联 SCSS，违反 CLAUDE_RULES.md SCSS 分离规范；与 index.scss 存在 4 处重复（.form-hint、::deep(.si-button--small)、::deep(.si-input--small .si-input__field)、.section-header 基类）
- **新建** `styles/S3ConfigForm.scss`（约 120 行）— 组件专属样式（.s3-config-form 包裹的 .section-header 扩展、.config-guide-panel、.form-grid、.form-group、.form-actions、.connection-result、.guide-fade-*），移除重复项
- **修改** `S3ConfigForm.vue` — 内联 SCSS 替换为双行 @use 导入（S3ConfigForm.scss + index.scss）
- **验证**：ESLint 0 error
