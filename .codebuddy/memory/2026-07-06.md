# 2026-07-06

## gitPush 仓库链接列表化改造

将 EditProjectDialog.vue 的"仓库链接"区块从固定 4 个 Input 平铺改为列表式 UI（仿"Git 远程仓库"区块），支持单独新增/编辑/删除，每次操作立即调 updateProjectMeta 持久化。

### 改动文件
- `src/features/gitPush/components/EditProjectDialog.vue`：模板替换为列表式（已配置列表+内联编辑+底部添加行）；脚本新增 repoLinkList/availablePlatforms/linkAddOptions computed、persistUrls/handleAddRepoLink/handleDeleteRepoLink/saveRepoLinkEdit/cancelRepoLinkEdit 函数；onMounted 新增 newLinkPlatform 初始化；save() 移除 urlInputs 四行
- `src/features/gitPush/styles/index.scss`：移除已无引用的 .gp-edit-urls/.gp-edit-url-row 样式

### 设计决策
- 数据模型保持 githubUrl/giteeUrl/giteaUrl/cnbUrl 四字段不变，每平台最多一个链接
- 每次操作立即保存（调 updateProjectMeta + emit("saved")），与 Git 远程仓库交互一致
- 复用现有 .gp-remote-list/.gp-remote-row/.gp-remote-add 样式类
- 添加行仅显示未配置平台（availablePlatforms computed），全部配置后隐藏添加行

### 验证
- ESLint 0 error
- i18n:verify 通过（1827 键对齐）
- tsc --noEmit 无类型错误

## S3Backup 输入框溢出修复

S3ConfigForm.vue 中 .form-input 缺少 `box-sizing: border-box`，导致 `width: 100%` + padding 时实际宽度超出父容器。修复 3 处：
- `.form-input` 添加 `box-sizing: border-box`
- `.form-group` 添加 `min-width: 0` 防 grid 子项撑破列宽
- `.password-input-wrapper` 添加 `box-sizing: border-box`
lint 通过。

## S3Backup 冗余审查与清理

8 个文件修改，移除约 80 行死代码/冗余，修复 6 处误导文案。四重验证通过。

### 移除的死代码
- `useS3Backup.performBackup()` — 从未调用（index.vue 自实现逐文件上传）
- `S3Client.upload()` / `S3Client.updateConfig()` — 无调用方
- `error` ref 及所有 `error.value =` 赋值（无读取方）
- `isConnected`/`isConnecting`/`initClient`/`performBackup` 从 composable return 移除
- 调试 `console.log`（s3Client.ts:438，每次请求泄露 accessKey 前缀）

### 冗余清理
- `BackupManager`：移除 `WorkspaceFile.size` + `fs.stat`（每个文件省一次 I/O）、移除无意义拷贝循环、移除 `_workspaceRoot` 未使用参数、`updateWorkspacePaths()` 改 `updateWorkspacePath()`
- `index.ts`：合并 `cachedWorkspacePath`/`cachedWorkspaceRoot` 为单一字段（两者永远相同值）
- `_workspaceRoot` 参数彻底移除（BackupManager 构造和 update 方法、index.vue 调用处）

### 误导文案修复
- i18n `backupHint`：zip 打包 → 逐文件上传
- i18n `confirmRestore`：覆盖数据 → 下载到本地目录需手动解压
- 新增 `downloadToLocalSuccess` 键（之前代码引用但缺失，回退硬编码中文）
- 删除 5 个废弃键：`packing`/`compressing`/`restoreSuccess`/`selectWorkspace`/`loading`
- README.md：移除"JSZip/DEFLATE 压缩"和"自动备份"等未实现描述

### 扩展建议（8 项，信息性）
自动定时备份、增量备份、Zip 压缩、备份加密、保留策略、一键恢复、配置导入导出、上传校验。详见 plan。
