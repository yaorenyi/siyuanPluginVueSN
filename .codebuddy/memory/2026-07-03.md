# 2026-07-03 工作日志

## gitPush 模块 — index.vue JavaScript 区域代码迁移

审查了 `index.vue` 的 `<script setup>` 区域（~1000 行），将散落在组件中的共享常量/函数迁移到类型层和工具层：

### 迁移至 `types/index.ts`
- **`STATUS_META`**：项目状态徽章元数据（color/label/icon），消除 `index.vue` 和 `EditProjectDialog.vue` 的重复定义
- **`STATUS_CYCLE`**：复用已有的 `PROJECT_STATUS_VALUES`，通过 `export { PROJECT_STATUS_VALUES as STATUS_CYCLE }` 重导出
- **`REMOTES`**：`PLATFORM_META` 的精简投影（含 remoteProp），供卡片和状态栏使用

### 迁移至 `utils.ts`
- **`hasAnyRemote(project)`**：判断项目是否配置了任何远程仓库的纯函数

### 修改的文件
| 文件 | 变更 |
|------|------|
| `types/index.ts` | +29 行：新增 STATUS_META / STATUS_CYCLE / REMOTES |
| `utils.ts` | +3 行：新增 hasAnyRemote() |
| `index.vue` | -42 行：移除本地定义，改用类型层/工具层导入 |
| `EditProjectDialog.vue` | -23 行：移除重复定义，改用类型层导入（含 PLATFORM_META 导入合并） |

### 验证结果
- ESLint：0 error
- tsc --noEmit：通过

---

## gitPush — 禁用卡片点击自动刷新

注释掉 `handleCardClick` 中 `handleRefresh(projectId)` 调用（index.vue L1350），点击卡片不再触发 6 项 git 操作。保留手动 Fetch 按钮、刷新全部按钮等所有其他入口。
- ESLint：0 error

修复了 12 个交互逻辑漏洞（高 6 + 中 5 + 低 1）。

### 变更统计（5 个文件）

| 文件 | 变更 |
|------|------|
| `GitPushManager.ts` | 漏洞 1/2/4/6：AbortController Map→数组管理、队列 signal 关联过滤、applyRemotesToProject 先清空再设置、ff-only 中文错误消息 |
| `useProjectCrud.ts` | 漏洞 5：toggleStar/setProjectStatus 加 try-catch 回滚 + showMessage |
| `index.vue` | 漏洞 3/7/8/10/11：@saved→loadProjects、6处confirm→通用ConfirmDialog、批量取消按钮、名称编辑错误处理+空值警告、stash/tag/merge 确认改用 showConfirm |
| `EditProjectDialog.vue` | 漏洞 4：增删改远程后追加 refreshRemotes 同步持久化 |
| `ConfirmDialog.vue` | 漏洞 9：v-html→具名 slot `<slot name="message">`，消除 XSS 隐患 |
| `PanelHeader.vue` | 漏洞 8：新增「取消」按钮 + cancelPushAll emit |

### 核心修复点
- AbortController：Map<string, AbortController> → Map<string, AbortController[]>，同项目多操作不覆盖，cancelOp遍历全部abort
- 全局队列：清空全部 `filter(()=>false)` → `filter(item => item.signal !== signal)` 精准过滤
- 远程同步：applyRemotesToProject 先清零再根据检测结果设置，EditProjectDialog 远程操作后调 refreshRemotes
- 乐观更新：toggleStar/setProjectStatus try-catch + 回滚 + showMessage
- ConfirmDialog：通用状态模式替代 6 处原生 confirm()，v-html 改 slot

### 验证结果
- ESLint：0 error
- tsc --noEmit：通过

---

## gitPush 模块 — SCSS 冗余代码合并

审查了 gitPush 全部 12 个 SCSS 文件，识别了 7 种跨文件重复模式。

### 新增 3 个 mixin（`_mixins.scss` +35 行）
| Mixin | 替代的重复代码 | 出现次数 |
|-------|--------------|---------|
| `popover-base` | 弹出层容器（position/background/border/radius/flex/padding/z-index） | 3 |
| `popover-item` | 弹出菜单项按钮（flex/gap/hover/item） | 3 |
| `row-divider` | 列表行 border-bottom + last-child 无边框 | 6 |

### 统一已有 mixin 调用（6 处）
- **`@include focus-ring`** 替代手动 border-color：`index.scss` × 2 + `WorkingTreePanel.scss` × 1
- **`@include text-ellipsis`** 替代手动 text-overflow 三件套：`BranchCommitList.scss` × 2
- **`@include gp-label-base`** 替代手动大写标签：`StatsPanel.scss` × 1 + `WorkingTreePanel.scss` × 1 + `BranchCommitList.scss` × 1

### 修改的文件（6 个）
| 文件 | 行数变化 |
|------|---------|
| `_mixins.scss` | +35 |
| `index.scss` | -30（popover/item 重构 + focus-ring ×2 + row-divider ×3） |
| `PanelHeader.scss` | -35（popover ×2 + item ×2 + 新增 @use mixins） |
| `StatsPanel.scss` | -10（row-divider + gp-label-base + 新增 @use mixins） |
| `WorkingTreePanel.scss` | -10（row-divider + focus-ring + gp-label-base） |
| `BranchCommitList.scss` | -9（text-ellipsis ×2 + gp-label-base + 新增 @use mixins） |
| **净减** | **~59 行** |

### 验证结果
- ESLint：0 error
- tsc --noEmit：通过（无新增错误）

---

## gitPush — 新增批量加载进度条

新增 `LoadProgress` 接口 + `runBatchWithProgress()` 包装函数，底部显示进度条（`加载中 3/12` + 进度条 + 项目名）。
- 改造 4 个 batchProcess 调用点（onMounted/watch分类/watch视图/silentRefreshAll）
- 样式：flex布局、vp-mono 字体、0.3s ease 过渡
- items ≤ 1 跳过进度，避免单项目闪烁；调整为 ≤1 后 2 个项目即可看到进度条
- **二次修复**（09:50）：移除门槛 + 400ms 最小展示时间 → 单项目刷新 (`handleRefresh`) 也改用 runBatchWithProgress → 进度条加高到 5px + 标签亮色强调
- 验证：Lint 0 error、tsc 通过
- 验证：Lint 0 error、tsc 通过

