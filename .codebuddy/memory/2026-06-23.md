# 2026-06-23

## skillLearning AI 生成增强
- 修改 `src/features/skillLearning/components/SkillDialog.vue` 的 `aiGenerate()` 函数
- Prompt 扩展：新增 `title`（润色题目）、`codeSnippet`（代码片段）、干扰项长度 ±30% 约束
- 响应解析新增：`parsed.title` → `form.title`、`parsed.codeSnippet` → `form.codeSnippet`
- maxTokens 600 → 1000
- 按钮 tooltip "AI 生成答案" → "AI 生成全部内容"
- 构建验证通过，零 lint 错误

## skillLearning 全面代码审查与重构

### 新增功能：闪卡对错率统计
- `SkillCard` 类型新增 `correctCount?: number`、`wrongCount?: number` 可选字段（向后兼容）
- `FlashcardView` emit `practice` 事件区分对错（新增 `isCorrect` 参数）
- `StatsView` 统计页新增：正确率环形图 + 正确/错误分布条
- `handleRate()` 中 `remembered` 评分计为正确，`fuzzy`/`forgot` 计为错误
- i18n 新增 4 个翻译键：`accuracyRate`、`correctCountLabel`、`wrongCountLabel`、`notPracticed`

### 存储层性能优化
- 移除 `SkillStorage` 中未被使用的 `getLanguages()`、`getCategories()` 死代码
- 新增 `updateReviewAndPractice()` 合并方法：复习数据 + 练习计数 + 对错计数 = 单次 `getAllCards()` 调用（原为独立两次，减少 50% I/O）
- `useSkillStorage` 改用乐观更新：`createCard`/`updateCard`/`deleteCard` 成功后直接操作 `cards` ref 数组，不再全量 `loadCards()`
- 新增 `incrementPracticeWithAccuracy()` 支持闪卡对错计数

### SCSS 去重
- 新建 `styles/_shared.scss`，包含 4 个 @mixin：
  - `shared-empty` — 空状态样式（消除 4 处重复）
  - `lang-tag-colors` — 语言标签颜色（消除 3 处重复）
  - `shared-progress-bar` — 进度条（消除 FlashcardView/ReviewView 重复）
  - `shared-code-block` — 代码块（消除 FlashcardView/ReviewView 重复）
- `ReviewView` 代码块 `max-height` 保留 180px 覆盖（原 200px → 180px）
- `StatsView` 硬编码 `1.5rem` → `24px`

### TS 去重
- 新建 `useFilteredCards` composable 提取 `SkillListView` 和 `FlashcardView` 共用的筛选/分页逻辑（支持 `enableSearch` 开关）
- `LANG_COLORS` 颜色映射提取到 `useLangLabel.ts` 统一管理

### 验证
- `pnpm i18n:verify` ✅ 1739 个键对齐
- `pnpm lint` ✅ 零错误
- 修改范围：14 个文件（5 新建 + 9 修改）

## 移除浮动工具栏子功能在超级面板中的独立条目
- 需求：translate（英译中替换）和 pronunciation（谐音翻译）是浮动工具栏的子功能，不应在超级面板中作为独立功能显示
- 修改 3 个文件：
  1. `src/features/config.ts` — 删除 pronuciation 和 translate 的 FEATURE_CONFIG 条目
  2. `src/features/index.ts` — 从 `_ConfigOnly` 白名单移除这两个 ID
  3. `src/config/icons.ts` — 删除 pronounceion 和 translate 的 FEATURE_ICONS
- 保留不变：settings 中的 `enableTranslate`/`enablePronunciation`、浮动工具栏核心逻辑、i18n
- 验证：`pnpm build` ✅、`pnpm validate:icons` ✅、零 lint 错误

## 将浮动工具栏子功能集成为卡片内子开关
- 需求：translate 和 pronunciation 应显示在浮动工具栏卡片内部，作为子功能开关（而非独立功能条目）
- 新增 `SubFeatureMeta` 接口到 `src/features/config.ts`（id/label/icon/color），`FeatureMeta` 新增 `subFeatures?: SubFeatureMeta[]`
- `floatingToolbar` 添加子功能：translate（英译中替换，mdi:translate，#0d9488）和 pronunciation（谐音翻译，mdi:volume-high，#f43f5e）
- `SubFeature` 接口添加到 `superPanel/types/index.ts`（含 enabled 字段），`Feature` 接口新增 `subFeatures?: SubFeature[]`
- `superPanel/index.vue`：features computed 映射 subFeatures 并解析 i18n + enabled 状态；新增 `toggleSubFeature` emit
- `FeatureCard.vue`：在描述后、按钮前渲染子功能行（icon + label + Switch），emit toggleSubFeature
- `feature-card.scss`：新增 `.feature-sub-features` / `.sub-feature-item` / `.sub-feature-icon` / `.sub-feature-label` / `.sub-feature-toggle` 样式
- `SuperPanelManager`：新增 `onToggleSubFeature` 回调，读取当前设置值后反转切换
- 子功能使用 `@iconify/vue` 的 `<Icon>` 直接渲染（不依赖 IconWrapper/FEATURE_ICONS）
- 验证：`pnpm build` ✅、`pnpm validate:icons` ✅、零 lint 错误

## FeatureCard 操作按钮改为默认显示 + 与开关并排
- 需求：操作按钮（如「打开压缩器」「打开管理器」等）之前 hover 才显示，现改为默认显示，并与右侧开关并排在标题行
- `FeatureCard.vue`：将 `.feature-actions` 从卡片底部移到 `.feature-header` 内部，位于 `.feature-info` 与 Switch 之间
- `feature-card.scss`：用 `.feature-header-actions` 替换 `.feature-actions`，移除 opacity:0/transform/border-top 等 hover 隐藏逻辑
- 同时修复 3 个 TS 问题：index.vue 的 toggleSubFeature emit 签名、FeatureCard 中未使用的 SubFeature 导入、types/index.ts 中未使用的 SubFeatureMeta 导入、selectStatus 中 "清空" 选项的类型断言
- 验证：`pnpm build` ✅、零 lint 错误
