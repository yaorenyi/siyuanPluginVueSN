# 2026-06-23

## skillLearning AI 生成增强
- 修改 `src/features/skillLearning/components/SkillDialog.vue` 的 `aiGenerate()` 函数
- Prompt 扩展：新增 `title`（润色题目）、`codeSnippet`（代码片段）、干扰项长度 ±30% 约束
- 响应解析新增：`parsed.title` → `form.title`、`parsed.codeSnippet` → `form.codeSnippet`
- maxTokens 600 → 1000
- 按钮 tooltip "AI 生成答案" → "AI 生成全部内容"
- 构建验证通过，零 lint 错误

## skillLearning 全面代码审查与重构

### 新增功能：闪卡对错率统计
- `SkillCard` 类型新增 `correctCount?: number`、`wrongCount?: number` 可选字段（向后兼容）
- `FlashcardView` emit `practice` 事件区分对错（新增 `isCorrect` 参数）
- `StatsView` 统计页新增：正确率环形图 + 正确/错误分布条
- `handleRate()` 中 `remembered` 评分计为正确，`fuzzy`/`forgot` 计为错误
- i18n 新增 4 个翻译键：`accuracyRate`、`correctCountLabel`、`wrongCountLabel`、`notPracticed`

### 存储层性能优化
- 移除 `SkillStorage` 中未被使用的 `getLanguages()`、`getCategories()` 死代码
- 新增 `updateReviewAndPractice()` 合并方法：复习数据 + 练习计数 + 对错计数 = 单次 `getAllCards()` 调用（原为独立两次，减少 50% I/O）
- `useSkillStorage` 改用乐观更新：`createCard`/`updateCard`/`deleteCard` 成功后直接操作 `cards` ref 数组，不再全量 `loadCards()`
- 新增 `incrementPracticeWithAccuracy()` 支持闪卡对错计数

### SCSS 去重
- 新建 `styles/_shared.scss`，包含 4 个 @mixin：
  - `shared-empty` — 空状态样式（消除 4 处重复）
  - `lang-tag-colors` — 语言标签颜色（消除 3 处重复）
  - `shared-progress-bar` — 进度条（消除 FlashcardView/ReviewView 重复）
  - `shared-code-block` — 代码块（消除 FlashcardView/ReviewView 重复）
- `ReviewView` 代码块 `max-height` 保留 180px 覆盖（原 200px → 180px）
- `StatsView` 硬编码 `1.5rem` → `24px`

### TS 去重
- 新建 `useFilteredCards` composable 提取 `SkillListView` 和 `FlashcardView` 共用的筛选/分页逻辑（支持 `enableSearch` 开关）
- `LANG_COLORS` 颜色映射提取到 `useLangLabel.ts` 统一管理

### 验证
- `pnpm i18n:verify` ✅ 1739 个键对齐
- `pnpm lint` ✅ 零错误
- 修改范围：14 个文件（5 新建 + 9 修改）

## 移除浮动工具栏子功能在超级面板中的独立条目
- 需求：translate（英译中替换）和 pronunciation（谐音翻译）是浮动工具栏的子功能，不应在超级面板中作为独立功能显示
- 修改 3 个文件：
  1. `src/features/config.ts` — 删除 pronuciation 和 translate 的 FEATURE_CONFIG 条目
  2. `src/features/index.ts` — 从 `_ConfigOnly` 白名单移除这两个 ID
  3. `src/config/icons.ts` — 删除 pronounceion 和 translate 的 FEATURE_ICONS
- 保留不变：settings 中的 `enableTranslate`/`enablePronunciation`、浮动工具栏核心逻辑、i18n
- 验证：`pnpm build` ✅、`pnpm validate:icons` ✅、零 lint 错误

## 将浮动工具栏子功能集成为卡片内子开关
- 需求：translate 和 pronunciation 应显示在浮动工具栏卡片内部，作为子功能开关（而非独立功能条目）
- 新增 `SubFeatureMeta` 接口到 `src/features/config.ts`（id/label/icon/color），`FeatureMeta` 新增 `subFeatures?: SubFeatureMeta[]`
- `floatingToolbar` 添加子功能：translate（英译中替换，mdi:translate，#0d9488）和 pronunciation（谐音翻译，mdi:volume-high，#f43f5e）
- `SubFeature` 接口添加到 `superPanel/types/index.ts`（含 enabled 字段），`Feature` 接口新增 `subFeatures?: SubFeature[]`
- `superPanel/index.vue`：features computed 映射 subFeatures 并解析 i18n + enabled 状态；新增 `toggleSubFeature` emit
- `FeatureCard.vue`：在描述后、按钮前渲染子功能行（icon + label + Switch），emit toggleSubFeature
- `feature-card.scss`：新增 `.feature-sub-features` / `.sub-feature-item` / `.sub-feature-icon` / `.sub-feature-label` / `.sub-feature-toggle` 样式
- `SuperPanelManager`：新增 `onToggleSubFeature` 回调，读取当前设置值后反转切换
- 子功能使用 `@iconify/vue` 的 `<Icon>` 直接渲染（不依赖 IconWrapper/FEATURE_ICONS）
- 验证：`pnpm build` ✅、`pnpm validate:icons` ✅、零 lint 错误

## FeatureCard 操作按钮改为默认显示 + 与开关并排
- 需求：操作按钮（如「打开压缩器」「打开管理器」等）之前 hover 才显示，现改为默认显示，并与右侧开关并排在标题行
- `FeatureCard.vue`：将 `.feature-actions` 从卡片底部移到 `.feature-header` 内部，位于 `.feature-info` 与 Switch 之间
- `feature-card.scss`：用 `.feature-header-actions` 替换 `.feature-actions`，移除 opacity:0/transform/border-top 等 hover 隐藏逻辑
- 同时修复 3 个 TS 问题：index.vue 的 toggleSubFeature emit 签名、FeatureCard 中未使用的 SubFeature 导入、types/index.ts 中未使用的 SubFeatureMeta 导入、selectStatus 中 "清空" 选项的类型断言
- 验证：`pnpm build` ✅、零 lint 错误

## everythingSearch 模块全面重构（20 项 issue）

### SCSS 分离（CLAUDE.md 强制规则）
- 创建 8 个 `styles/*.scss` 文件，对应每个组件的内联样式提取（AdvancedHelpPanel/DialogFooter/DialogHeader/ResultItem/SearchBar/SearchOptions/SearchResults/ServiceWarning）
- 所有 .vue 文件 `<style>` 块仅保留 `@use "../styles/Xxx.scss";` 一行
- 修复 `SearchResults.vue` 非 scoped 样式泄漏（`.vp-empty` 样式全局污染）

### SCSS 去重与 Token 统一
- 删除 6 个文件中本地重复声明的 `$vp-mono`（统一从 `@/variables.scss` 继承）
- 硬编码值替换为全局 Token：`gap: 4px`→`$spacing-1`、`padding: 8px`→`$spacing-2`、`font-size: 11px`→`$font-size-xs`、`border-radius: 4px`→`$radius-sm` 等

### 冗余代码消除（8 项）
- `index.vue`：删除冗余 `loadConfig()` 包装函数（与 `loadConfigFromPlugin` 合并）；删除模板双重 `v-if="visible"`（外层 overlay 已足够）；删除空桩 `handleItemClick`；`listAvailableDrives` 去除无用 `async`
- `SearchBar.vue`：`searchButtonText` computed 改为常量 `SEARCH_BUTTON_TEXT`
- `types/index.ts`：移除无用 `_EverythingSearchResult` 别名
- 统一组件命名：`SearchOptionsPanel`→`SearchOptions`

### 性能优化（4 项）
- `SearchOptions.vue`：3 个不变 `computed`（`maxResultsOptions`/`debounceOptions`/`sortOptions`）提升为模块级 `MAX_RESULTS_OPTIONS`/`DEBOUNCE_OPTIONS`/`SORT_OPTIONS` 常量
- `SearchResults.vue`：v-for `:key` 移除冗余 `index` 后缀
- 简化 3 层事件转发链：移除 ResultItem→SearchResults→index.vue 的 `click` 空事件链

### 代码质量（4 项）
- `api.ts`：提取 `getElectronShell()` 懒加载单例函数，消除 `openFile`/`showInExplorer` 中重复的 `window.require("@electron/remote")`
- `showInExplorer` 从 `async`→同步（`shell.showItemInFolder` 本身同步）
- `formatDate` 硬编码 `"zh-CN"`→`navigator.language || "zh-CN"` 动态获取浏览器 locale
- `index.vue` import 分组整理：去除冗余注释行

### 验证
- `pnpm build` ✅、零 lint 错误
- 修改文件：18 个（8 新建 SCSS + 10 修改）
- 功能行为不变，仅结构性改进

## everythingSearch 删除自动搜索开关，默认保留自动搜索
- 删除 `SearchOptions.vue` 中「自动搜索」Switch 开关按钮
- 移除延迟下拉框的 `v-if="options.autoSearch"`（始终显示）
- `types/index.ts`：从 `SearchOptions` 接口删除 `autoSearch: boolean`
- `types/storage.ts`：从 `DEFAULT_OPTIONS` 删除 `autoSearch: true`
- `index.vue`：移除 SearchBar/SearchResults 的 `:auto-search` prop；`debouncedSearch()` 和 `handleDriveChange()` 移除 `options.autoSearch` 条件判断
- `SearchBar.vue`：删除 `autoSearch` prop，Enter 键始终触发搜索（改为 `event.key === "Enter"` 无条件 emit）
- `SearchResults.vue`：删除 `autoSearch` prop，`emptyHintText` 从 computed 改为常量 "输入关键词自动搜索本地文件"，移除未使用的 `computed`/`props` 导入
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：6 个

## everythingSearch 删除盘符选择
- `SearchOptions.vue`：删除盘符 Select + 刷新按钮 UI、`driveOptions` computed、`handleDriveChange`/`handleRefreshDrives` 函数、`availableDrives` prop、`driveChange`/`refreshDrives` emit；移除未使用的 `computed` 导入；修复 `sort` 的类型断言（`as string`→`as unknown as SearchOptions['sort']`，因删除 `selectedDrive: string` 后联合类型收窄）
- `index.vue`：删除 `availableDrives` ref、`listAvailableDrives()`、`refreshDrives()`、`handleDriveChange()` 三个函数；删除 `loadConfig()` 中的 `selectedDrive` 兜底赋值；`handleSearch()` 移除 `selectedDrive` 前缀拼接逻辑（直接传 `query`）；模板移除 `:available-drives`、`@drive-change`、`@refresh-drives` 三个绑定；`onMounted` 移除 `listAvailableDrives()` 调用
- `types/index.ts`：`SearchOptions` 接口删除 `selectedDrive: string`
- `types/storage.ts`：`DEFAULT_OPTIONS` 删除 `selectedDrive: ""`
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：4 个

## everythingSearch 结果列表增加删除文件按钮
- `api.ts`：新增 `deleteFile()` 函数，通过 `getElectronShell().moveItemToTrash()` 移入回收站（安全删除，非直接永久删除）
- `ResultItem.vue`：新增删除按钮（`#iconTrashcan` 图标），新增 `delete` emit 和 `handleDelete` 处理函数
- `SearchResults.vue`：新增 `itemDelete` emit，新增 `handleItemDelete` 转发函数
- `index.vue`：新增 `handleItemDelete()` — 调用 `deleteFile()`、从 `searchState.results` 中移除该项、结果为空时状态切为 `"empty"`、成功/失败均提示
- `ResultItem.scss`：新增 `&__action--delete:hover` 样式（红色 #e74c3c，破坏性操作视觉区分）
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：5 个

## everythingSearch 增加文件大小过滤
- `types/index.ts`：`SearchOptions` 新增 `minSize`/`minSizeUnit`/`maxSize`/`maxSizeUnit` 四个字段
- `types/storage.ts`：`DEFAULT_OPTIONS` 新增默认值（均为 0 + 'KB'）
- `SearchOptions.vue`：新增文件大小过滤行（两个 number input + 两个 单位 Select，中间 `-` 分隔符）；新增 `SIZE_UNIT_OPTIONS` 常量、`onMinSizeInput`/`onMaxSizeInput` 处理函数
- `index.vue`：`handleSearch()` 中拼接 `size:>NKB` / `size:<NMB` 到查询字符串（Everything 原生语法）
- `SearchOptions.scss`：新增 `&__item--size`、`&__size-input`（含隐藏数字箭头）、`&__size-sep` 样式
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：5 个

## everythingSearch SearchOptions 布局重排 + 文件大小默认值修复
- **模板重排**：全部 Switch 加 `label-before`（标签前置），删除 `vp-options__group` 嵌套，改为三行布局（`vp-options__row` 容器）：
  - 行1：区分大小写/全词匹配/匹配路径/正则/高级模式 5 个开关（标签在前，视觉统一）
  - 行2：数量/延迟/排序+↑↓ 三个 Select
  - 行3：大小 ≥[数值]KB - ≤[数值]KB
- **size-input 修复**：`:value="options.minSize || ''"` → `:value="options.minSize"`（0 也会显示而非空白），删除 placeholder 属性
- **SCSS 重写**：删除 `__group` 样式；新增 `__row`（flex 行容器）；`__size-input` 尺寸对齐 Select small（28px 高，`$radius-base` 6px，`$font-size-sm` 14px，宽 48px，居中文字）；`__key` 加 Codex 标签规范
- 持久化已通过 `watch(..., { deep: true })` 确认生效
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：2 个（SearchOptions.vue + SearchOptions.scss）

## everythingSearch 选项变更后自动重新搜索
- 问题：切换开关/Select/大小输入后只保存配置，不重新搜索（需手动输入搜索词才触发）
- 修复：`handleOptionUpdate()` 末尾新增 `if (searchQuery.value.trim()) { handleSearch() }`，当前有搜索词时立即执行搜索
- 影响范围：所有选项（Switch/Select/大小输入）变更后只要有搜索词就自动刷新结果
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：1 个（index.vue）

## 完整移除 src/features/base64Image/ 旧独立功能模块
- 删除 `src/features/base64Image/` 整个目录（含 index.vue（903行）、index.ts（no-op）、7 个子组件、样式、类型、README）
- 从 6 个文件移除了 base64Image 的注册引用：
  1. `src/features/index.ts` — 移除 `registerBase64Image` 导出 + `_ConfigOnly` 白名单条目
  2. `src/index.ts` — 移除 `registerBase64Image` 导入和 `if (s.enableBase64Image)` 注册调用
  3. `src/features/config.ts` — 从 `FEATURE_CONFIG` 移除 `base64Image` 条目
  4. `src/config/settings.ts` — 从 `PluginSettings` 接口和 `DEFAULT_SETTINGS` 移除 `enableBase64Image`
  5. `src/config/icons.ts` — 从 `FEATURE_ICONS` 移除 `base64Image` 图标配置
  6. `src/features/superPanel/types/index.ts` — 从 `ACTION_EVENT_MAP` 移除 `openBase64Image`
- 保留不变：`src/i18n/{zh_CN,en_US}/base64Image.json`（toolCollection/tools/base64Image/ 仍在使用）、`src/features/toolCollection/tools/base64Image/`（已迁移版本）
- 验证：`pnpm lint` ✅、`pnpm validate:icons` ✅（119 图标）、`npx tsc --noEmit` 无新增错误

## everythingSearch 新增常用关键字功能（增删查 + 持久化）
- **类型扩展**：`SearchOptions` 新增 `frequentKeywords: string[]`；`DEFAULT_OPTIONS` 默认值 `[]`
- **新建 `FrequentKeywords.vue`**：chip 行展示常用关键字组件
  - 每个关键字显示为圆角 chip（`$radius-full`），点击→将关键字填入搜索框
  - chip 末尾 × 按钮删除（`@click.stop` 阻止冒泡到 chip 的 insert 事件）
  - 空列表显示斜体提示 "暂无，点击 + 添加"
  - `+` 按钮（虚线边框圆形）→ 切换为内联输入框 + "确定"/"取消" 按钮
  - 添加时自动去重（已存在则忽略）；Enter 确认 / Escape 取消
  - 不支持直接编辑（删除+重新添加即可）
- **新建 `FrequentKeywords.scss`**：chip 圆角胶囊样式（14px 高内边距 2px 8px）；删除 × 圆形按钮 hover 显色；`+` 按钮 24px 圆形；输入行 26px 高度对齐；标签大写 10px bold 0.06em 间距
- **index.vue 集成**：模板插入 `<FrequentKeywords>` 在 SearchOptions 与 AdvancedHelpPanel 之间；新增 `handleKeywordInsert`（覆盖搜索词）/ `handleKeywordAdd`（push 数组）/ `handleKeywordDelete`（splice）三个处理函数
- **持久化自动生效**：`frequentKeywords` 属于 `options` 的 reactive 字段，已有 `deep:true` watch 自动保存
- 验证：`pnpm build` ✅、零 lint 错误
- 修改文件：5 个（2 新建 + 3 修改）

## floatingBox 功能模块全面重构（7 项优化）
- **跨功能导入修复**：3 处违规（flashcardReading.ts/passwordVault.ts/textDiff.ts 直接导入其他 feature）改为 `emitCustomEvent` 事件模式 → App.vue 中枢调度
- **巨型组件拆分**：`PromptsModal.vue` 831 行 → 1 个容器（~100 行）+ 4 个子组件（PromptsGrid/PromptFormModal/CategoryManageModal/DeleteConfirmModal）+ 2 个 composable（usePrompts/useCategoryManager）
- **SCSS 架构重构**：`_mixins.scss` 提取共享 mixins/variables；`PromptsModal.scss` 603 行拆分为 5 个 partial（_modal/_grid/_form/_category/_responsive）；消除 `@use "./index"` 副作用导入
- **冗余代码消除**：删除 `promptsTool` 别名函数、`getI18nMap()` 48 行手动 fallback 字典
- **i18n 补全**：floatingBox.json 新增 12 个缺失键（passwordVault/flashcardReading/refreshFiletree/refreshTag/refreshUI 等）
- **flashcardReading/index.ts**：新增 `toggleFlashcardDialog` 导出，供 App.vue 中枢调度使用
- **附带修复**：`gitPush/index.vue:773` 模板字面量 `{${e.index}}` 花括号解析错误 → 字符串拼接 `'{' + e.index + '}'`
- **验证**：`pnpm lint` ✅、`pnpm i18n:verify` ✅ 1750 keys、`npx tsc --noEmit` 零新增错误
- 修改文件：22 个（12 新建 + 10 修改）

## prompts/skills 功能从 floatingBox 独立提取
- **目标**：提示词库功能代码从 `floatingBox/` 迁移到独立的 `src/features/skills/`，对齐 textDiff 的事件驱动模式
- **新建** `skills/` 目录（17 个文件）：`index.ts`(registerFeature)、`index.vue`(容器)、`types/`(Prompt/PromptCategory/PromptContent/SkillsStorage)、`composables/`(usePrompts/useCategoryManager)、`components/`(PromptsGrid/PromptFormModal/CategoryManageModal/DeleteConfirmModal)、`styles/`(7 个 partial + index.scss)
- **floatingBox/tools/prompts.ts**：从直接创建 Modal 改为 `emitCustomEvent("openPrompts")`
- **App.vue**：新增 `"openPrompts"` 监听 → 调用 `showSkillsModal(plugin)`
- **注册链**：8 步注册完成（`features/index.ts` _Registered 加 "skills" + 导出、`_ConfigOnly` 移除 "skills"、`src/index.ts` registerSkills 调用）
- **清理**：删除 floatingBox 下已迁移的 14 个旧文件（5 组件 + 2 composable + 7 SCSS）+ 精简 `floatingBox/types/index.ts`（移除 Prompt 类型）+ 删除 `floatingBox/types/storage.ts`
- **验证**：`pnpm lint` ✅、`pnpm i18n:verify` ✅ 1760 keys、`npx tsc --noEmit` 零新增错误
- 修改文件：31 个（17 新建 + 14 删除 + 3 修改）

## docAnalysis 模块代码审查与重构

### SCSS Token 迁移（CLAUDE_RULES.md 合规）
- `_codex-tokens.scss`：删除本地 `$da-mono`/`$da-radius` 声明（值完全等同于全局 `$vp-mono`/`$vp-radius`），新增 `@use '@/variables.scss' as *;` 统一引入全局 Token
- 6 个 SCSS 文件（index/StatsOverview/DocListItem/FilterSettings/AttrsPanel/DateInput）：全部 `$da-radius`→`$vp-radius`、`$da-mono`→`$vp-mono`（共计 40 处替换）

### 死代码清理
- 删除 `_codex-tokens.scss` 中 `da-input-base`（28行）和 `da-close-btn`（18行）两个从未被 `@include` 引用的死 mixin

### 硬编码值替换
- 41 处 `font-size` 硬编码批量替换为全局 Token：
  - `font-size: 14px` → `$font-size-sm`（11 处）
  - `font-size: 16px` → `$font-size-base`（7 处）
  - `font-size: 12px` → `$font-size-xs`（23 处）

### StatsOverview.vue 重复计算消除
- 提取 `_healthBreakdown` computed：`healthPct` 和 `healthTooltip` 共享的 5 个中间值（`excessDupes`/`noBmExclude0B`/`depthGt7`/`wcGt20000`/`issues`）只计算一次，两个原有 computed 改为消费该对象

### index.vue IntersectionObserver 简化
- `setupObserver()` 中移除冗余 `requestAnimationFrame`（Vue watch 触发时 DOM 已更新），直接 `observer.observe(sentinelRef.value)`

### 验证
- `pnpm lint` ✅、`npx tsc --noEmit` 零新增错误、`pnpm build` ✅（无 Undefined variable 错误）
- 修改文件：9 个（1 删除死代码行 + 7 SCSS + 1 Vue 组件）

## docAnalysis 平台动态管理（PLATFORM_META 改为可增删改）

### 改造目标
将硬编码的 `PLATFORM_META` 常量改为响应式 ref，支持用户动态增删改平台配置，持久化到 PluginStorage。

### 架构变更
1. **`types/index.ts`**：新增 `PlatformMeta` 接口导出
2. **`types/storage.ts`**：新增 `DEFAULT_PLATFORM_META` 常量 + `DocAnalysisStorage.platformMeta` TypedStorage 槽位
3. **`composables/useDocAnalysis.ts`**：
   - `PLATFORM_META` 从 `const` 数组改为 `ref<PlatformMeta[]>(DEFAULT_PLATFORM_META)`
   - `ALL_PLATFORMS` 从静态计算改为 `getAllPlatformsMask()` 动态函数
   - 新增 `loadPlatformMeta()` / `savePlatformMeta()` 函数（异步读写存储）
   - 所有内部引用 `PLATFORM_META` → `PLATFORM_META.value`
   - 重新导出 `DEFAULT_PLATFORM_META`
4. **`utils/platformPublish.ts`**：全部 4 个工具函数改为接收 `platformMeta: PlatformMeta[]` 参数（消除对模块级常量的直接依赖）
5. **`components/AttrsPanel.vue`**：`PLATFORM_META` → `PLATFORM_META.value`，工具函数调用传入 `PLATFORM_META.value`
6. **`components/StatsOverview.vue`**：`PLATFORM_META.find` → `PLATFORM_META.value.find`
7. **`index.vue`**：解构新增 `loadPlatformMeta`/`savePlatformMeta`；`onMounted` 先加载平台配置；`activePlatformName` computed 使用 `.value`

### 新增组件：PlatformManageModal.vue
- 弹窗内列表显示所有平台，每行可编辑 ID/名称/匹配关键词/URL
- 上移/下移/删除按钮 + 新增平台 + 恢复默认
- ID 重复/空值实时校验，保存前总校验
- 通过 `savePlatformMeta` prop 回调持久化
- 平台过滤栏新增齿轮按钮（`platform-manage-btn`）打开弹窗
- 保存后自动重新分析

### 新增样式
- `styles/PlatformManageModal.scss`：弹窗 overlay/panel/header/body/footer 完整样式（280 行）
- `styles/index.scss`：新增 `.platform-manage-btn` 管理按钮样式

### 验证
- `pnpm lint` ✅、`npx tsc --noEmit` 零新增错误、`pnpm build` ✅

### 修改/新增文件汇总
- 修改：`types/index.ts`、`types/storage.ts`、`useDocAnalysis.ts`、`platformPublish.ts`、`AttrsPanel.vue`、`StatsOverview.vue`、`index.vue`、`styles/index.scss`
- 新增：`PlatformManageModal.vue`、`styles/PlatformManageModal.scss`

## PlatformManageModal UI 重构为类表格布局

### 问题
原始布局：每行 5 列卡片 + 竖向 4 个图标按钮 + 拖拽手柄，720px 内极挤，视觉层级不清。

### 改进
1. **列头导航**：新增 `platform-table-header` 显示"可见/名称/ID/匹配关键词/发布URL/排序删除"
2. **行内直输**：输入框默认透明无边框，hover 显示边界，focus 高亮（`box-shadow`），编辑状态一目了然
3. **横向操作栏**：↑↓✕ 并排排列，移除冗余竖向排列和拖拽手柄
4. **Subtle 分割线**：替代厚重卡片边框，行 hover 淡灰高亮
5. **宽度从 720→800px**，列宽精确约束：col-vis=36/col-name=90/col-id=75/col-actions=72，matchers+url 等分剩余
6. **头部优化**：平台计数 badge，标题+图标分离
7. **SCSS 全部重写**：从 285 行精简至 190 行，删除 card/margin-bottom/drag 等冗余样式

### 验证
- `pnpm lint` ✅、`pnpm build` ✅
- 修改文件：`PlatformManageModal.vue` + `PlatformManageModal.scss`
